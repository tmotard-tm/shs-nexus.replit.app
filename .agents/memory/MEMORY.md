- [LOA Recovery department scoping](loa-recovery-scoping.md) — agent visibility rides on department/queue access (no flag); cross-queue strip must filter to accessible modules; LOA `department` labels need substring→module mapping, not `departmentToQueueModule`.
- [LOA queue-item data shape](loa-queue-item-shape.md) — LOA items use a flat data shape (no `data.source`/`technician` wrapper); Assets queue UI silently drops them unless `isItemFromSync()` + `parseTechData()` are taught the shape.
- [Assets Queue truck-number candidate fallback](assets-queue-truck-candidate-fallback.md) — nexus-data join needs multiple truck-number sources per item (incl. rosterContact.lastKnownTruckLu); distinguish "no candidate" vs "no matching row" via getNexusMatchStatus().
- [LOA Recovery duplicate prevention](loa-recovery-duplicates.md) — startup+scheduler race doubled cases; durable guard is a partial unique index on open `(workflow_id,department)` + ON CONFLICT DO NOTHING, not just a mutex; Fleet lane column='Fleet Management' but data.lane='FLEET'.
- [LOA SOP timeline & recovery signal](loa-sop-recovery-signal.md) — recovery-initiated = real Fleet sibling (no self-status fallback); paused = real recoveryPaused flag (not date guess); outreach steps never claim "Sent"; cross-lane feed must be queue-access gated.
- [Autoscale listen-first](autoscale-listen-first.md) — deploy promote fails ("port never opened, expected 5000") if heavy DB/Snowflake bootstrap is awaited before server.listen(); defer it to a post-listen runStartupBootstrap().
- [VIN auto-decode on Create Vehicle](vin-decode-create-vehicle.md) — NHTSA vPIC fills Year/Make/Model/AssetType; AssetType is a constrained-6 heuristic that returns "" when uncertain; spinner uses a seq ref, not a cancelled flag.
- [Vehicle district update](vehicle-district-update.md) — per-vehicle district fan-out to TPMS/WMS/Holman; Holman prefix=last-4 on UPDATE (not create-flow's full district); only mirror cache when all 3 succeed.
- [BYOV/vehicle number reservation](byov-number-reservation.md) — create reserves number via active-row + partial-unique-index before fan-out; VIN discriminates retry vs collision; stale reclaim MUST be CAS, not blind id update.
- [Neon driver deserialization quirks](neon-http-driver-boolean-pitfall.md) — one-shot neon() HTTP misreads booleans (use app pool driver); app pool returns DATE as 'YYYY-MM-DD' strings, so slice(0,10) date math is safe.
- [Drizzle pg array params](drizzle-pg-array-params.md) — raw sql `= ANY(${jsArray})` via the pg pool driver → runtime "malformed array literal"; bind per-element with sql.join IN-list.
- [communication_logs.sent_by FK trap](communication-logs-sentby-fk.md) — system sends must pass sentBy=null (actor→metadata.sender); a non-user string throws after the email sent, silently losing the log + *_sent_at → daily re-fire.
- [Holman 202 ≠ applied (assign + district)](holman-assignment-verification.md) — 202/errorCount-0 = "queued", not applied; trust post-sync cache. Same driver failing repeatedly (UNKNOWN) while others succeed on the same vehicle = Holman-side record issue, not a Nexus bug. District change is verified async via fleet sync; sync FREEZES cache.district so verifyFromFleetData is the only authoritative completer — never optimistic-mirror district.
- [Startup init kills route registration](startup-route-registration.md) — an unbounded `await` between app.get/post in registerRoutes 404s every later route group; detach optional init. Startup DDL FK'ing a discovery-written table hangs on autoscale — skip-if-exists + DB lock_timeout/statement_timeout, not JS timeouts.
- [Holman district payload traps](holman-district-payload.md) — district submit must OMIT assignedStatusCode (string(1)); stored holman_submissions.payload has tracking-only targetPrefix/targetDistrict that must be stripped before any replay; 202≠accepted.
- [Neon WS drop → empty-message 500](neon-ws-drop-empty-message-500.md) — heavy Neon aggregator routes 500 {"message":""} on transient WS drop (closeCode 1006, empty ErrorEvent.message); serve bounded-stale cache only for transient drops.
- [Partial refresh must never clobber last-good cache](partial-refresh-clobber.md) — incomplete sweep results keep/rehydrate complete data + short retry deadline, never overwrite for the full TTL; self-fetching localhost routes 401 in prod.
- [AMS scorecard zeros vs pending](ams-scorecard-pending-state.md) — fixed-bucket-list + `?? 0` defaults render a misleading all-zeros card on 503/cold sweep; gate display on data&&activeSweepComplete, retry(5xx-only)+poll-until-complete, AbortController-cancel slow AMS pages.
- [TPMS /techinfo accepts a truck number](tpms-techinfo-truck-lookup.md) — GET /techinfo/{id} returns the assigned tech for a TRUCK number too, not only Enterprise ID; code comment/string claiming "no truck lookup" is WRONG (Postman example /techinfo/023132); truck→tech need not be cache-only.
- [AIMS authority semantics](aims-authority-semantics.md) — OWNERLDAPID (not LDAPID, which disagrees ~60%) is the assign authority; confirm by truck# not owner-ldap (termed owner 404s); TPMS "not assigned" = HTTP 400 "No Data Found".
- [techsupdatedafter blind to assignments](tpms-techsupdatedafter-assignment-blindness.md) — feed tracks profile/roster edits only; truck assign/unassign (UPDATE_TRUCK_OWNER→AIMDAO) never appears; reconcile assignments via live GET /techinfo (or Snowflake TRUCK_LU), not the feed.
- [TPMS↔Holman number matching](tpms-holman-number-matching.md) — TPMS pads truck #s, Holman doesn't; always match canonical on BOTH sides; vehicle_nexus_data legacy dup-format rows → read via variants, dedupe canonical.
- [Fleet reconciliation dry-run](fleet-reconciliation-dryrun.md) — tier-3 backstop live facts: AMS paginate via envelope `total` (cap 1000); Holman vehicle# is `holmanVehicleNumber` not `clientVehicleNumber`; baseline vs live (AMS ghost ~892 live, Holman lifecycle ~93 scoped).
- [Fleet reconciliation backfill end-state](fleet-reconciliation-backfill-endstate.md) — post-drain mix is NOT all-verified; held/skipped/verification_pending are correct-by-design (authority-contested, CAS-drift, async sync), not failures; applying self-heals after 10-min lease; console workflows can auto-restart.
- [Holman PO refresh liveness](holman-po-refresh-liveness.md) — listing GET is live per-request; KPI token is TEMPLATE-stable (same across logins ≠ frozen); API-postback approvals linger on the grid ~30–85 min, manual portal approvals clear <6 min.
- [Holman decision confirm scoping](holman-decision-confirm-scoping.md) — confirm reads judge ONLY acted-on lines (prior resubmit rounds stay locked with their old decision); line absent from render = indeterminate, never confirmed; grid absence is the real "applied" signal.
- [Holman PO reopen guard](holman-po-reopen-guard.md) — rental extensions re-authorize the SAME PO# at the same $0.00; amount-only reopen hid them forever; fix = grace/resubmit reopen + grid_last_seen_at sighting stamp.
- [Holman portal prod risks](holman-portal-prod-risk.md) — VRM rental-PO: headless Chromium (binary may be absent in autoscale image), live writes armed by inverted HOLMAN_DECISION_DRY_RUN (a global Secret → applies to prod too, not per-env), access gated to a VRM-local username allowlist (jmorga1/handers), not RBAC.
- [Fleet Ops pre-mortem risks](fleet-ops-premortem-risks.md) — non-obvious fleet failure modes: write-through divergence (downstream applied, local failed), assign/unassign SWALLOWS Holman submit-persist failure (district surfaces it), no idempotency key, sync clobbers tech fields (only district frozen), frozen-district deadlock.
- [Long commands & typecheck in Replit env](replit-long-commands-typecheck.md) — full tsc = `npm run check`, ZERO new errors vs ~224 baseline; workflow cap 15/10 blocks new workflows → foreground `timeout 280` run; PTC lacks setTimeout (shellExec sleep).
- [Architect review result-buffer truncation](architect-review-buffer.md) — architect() echoes relevantFiles+gitDiff into a ~35KB result; large files truncate the verdict. Review changes via includeGitDiff:true + empty relevantFiles + inlined schema facts; read result tail.
- [tpms_cached_assignments phased retirement](tpms-cached-assignments-retirement.md) — reads on tpms_tech_profiles; writes now FROZEN (FREEZE_TPMS_CACHE_WRITES); don't drop schema until legacy readers verified. Truck→tech = canonical ltrim-zeros match, most-recent wins.
- [AIMS/Snowflake authority](aims-snowflake-authority.md) — AIMS_TRUCK_INFO is historical (filter max FILE_DATE→DELIND=0 for active); TEST_DB default inert (fully-qualify); ext systems = single shared prod instances; date header=UTC≠ET.
- [All Vehicles roster mirror](all-vehicles-mirror.md) — /api/fs/all-vehicles serves 4 Snowflake sources from a daily PG mirror to kill the Neon WS 503; mirror MUST be a raw transport cache (no dedupe, unassigned kept raw) — byte-for-byte parity beats a normalized schema.
- [Snowflake sync lock](snowflake-sync-lock.md) — ONE advisory lock ('fleetscope-mirror-sync') serializes ALL heavy Snowflake roster reads (mirror/TPMS/AMS); new readers must join it, wrap only the read, never nest.
- [Security-questions fail-open](security-questions-fail-open.md) — client must NOT force the SQ prompt on a transient status-check failure; failing closed caused random re-prompts for already-enrolled users.
- [dry_run holds active-target lock](dry-run-active-target-lock.md) — materialized dry_run items (status=queued) reserve the recon {system,truck,field} lock and silently block real canary/nightly/backfill writes; give them a non-active status.
- [WMS cost-center update safety](wms-cost-center-update-safety.md) — getTruck/getAllTrucks expose only a 13-field projection (no locationId/isActive); derive identity from truck#; updateTruck merges (only costCenter changes), non-projected NetSuite fields are unobservable+irrelevant.
- [Reconciliation auto-apply gate](reconciliation-auto-apply-gate.md) — Automate toggle must stay fail-safe OFF; Apply = real Holman/WMS/AMS writes; never auto-apply on materialize halt/startup; app_settings absence = OFF, not crash.
- [tpms_tech_profiles.truckNo refresh sources](tpms-truckno-refresh-sources.md) — authoritative truck writer is the 7:30 AM LIVE per-tech TPMS call, NOT the nightly TPMS_EXTRACT snapshot (which lags post-midnight moves ~24h); feed is move-blind → don't write truckNo from it, and don't source truckNo from the snapshot.
- [District cost-center auto-seed disabled](district-cost-center-autoseed.md) — keep DISTRICT_COST_CENTER_AUTO_SEED_ENABLED=false; auto-seed has no denylist so it resurrects admin-deleted/dirty districts (3132/3580). Manual-only via force trigger.
- [AMS Current Location source](ams-current-location-source.md) — fleet-card CurLoc must come from full-fleet searchVehicles cache (batchFetchAmsCurrentLocation), NOT ams_vehicles_cache.rawResponse (tech-shaped, no CurLoc); chain: full-fleet→DB-cache→Holman, never delivery addr.
- [AMS truck-status-map client resilience](ams-truck-status-map-client-resilience.md) — endpoint 503s by design while AMS cache warms; shared client query MUST retry(5xx/network)+poll, never retry:false, or all card pills/labels blank until manual refresh.
- [AMS user-updates write contract](ams-user-updates-contract.md) — POST user-updates validates enum fields (color/truckStatus/…) by NAME not UniqueID; Edit dialog must id→label convert + send only changed fields (re-sent stale color id 500'd "Invalid color").
- [Rental Ops → Fleet Scope sync](rental-ops-fleet-scope-sync.md) — destructive fs_trucks consolidate needs a platform Scheduled Deployment (autoscale kills in-process timers); watermark = completed sync_logs row type 'rental_ops_fleet_scope', NOT fs_rental_imports (manual weekly path).
- [App Launcher icons are DB rows](app-launcher-icons.md) — external_apps.logo_url not code; dev/prod separate DBs; insert-only seed won't overwrite on republish → value-guarded self-heal UPDATE fixes prod (prod DB is read-only via tools).
- [Comms module data restore after merge](comms-module-restore-after-merge.md) — merging the comms side-task brings code, not its fs_comms_* rows; re-run cutover per DB in order sync→migrate→sync (2nd sync's enrich+auto-archive keys unmatched to original tech & archives non-current). Orphan tsx proc → port 5000 EADDRINUSE.
- [Comms dev/prod split-brain](comms-dev-prod-split-brain.md) — "previews but empty threads" = check msgs_linked; interrupted dev-from-prod refresh strands messages; heal script snapshots fs_comms_* atomically + neutralizes copied send queue.
- [LOA outreach 2-day cadence cap](loa-outreach-cadence-cap.md) — max 2 distinct send days per cycle; reply/form/cap stop permanently, staff re-enable resets counter; backfill must be column-add-guarded or restarts re-cap re-enabled techs.
- [Master Fleet Communications Module](master-fleet-comms.md) — LDAP-keyed team-SMS inbox (fs_comms_*); dark-rollout flag, autoscale schedulers, WS payload.type override, contacts-sync ts trap; inbox rental-flag + bulk join by LDAP (not truck#).
- [Prod sync schedule reality](prod-sync-schedule-reality.md) — in-process sync-scheduler timers barely fire on autoscale (watermark polls NEVER ran in prod); audit via sync_logs + max(updated_at), not the coded schedule.
- [TPMS tech-specialty tier-3 snapshot](tpms-tech-specialty-snapshot.md) — TPMS_EXTRACT_LAST_ASSIGNED = last tech EVER on truck (never clears); tier-3 fallback shows it as current assignee → ghost assignments; live-verify via /techinfo/{truckNo} first.
- [OpenAI keys degrade silently](openai-keys-fail-soft.md) — both OpenAI features are fail-soft; out-of-quota keys pass /v1/models but 429 on completions, so "broken AI summaries" = check billing first, not code.
- [Comms API dedupe guard](comms-api-dedupe-guard.md) — machine API sends drop identical (digits,body,category) within 24h; allowDuplicate = escape hatch; UI sends exempt; keep checks sargable on phone_digits.
- [Batch outbound-call safety](batch-outbound-call-safety.md) — never await a phone-call batch in the handler (proxy kill → retry → double-dial); real fix = DB dedup guard incl. timeout-ambiguous dials logged guard-visible.
- [Dev server port contention](dev-server-port-contention.md) — "didn't open port 5000" despite serving = duplicate `npm run dev` from a Shell/Codex session; check proc parent chain + orphans before retrying restarts.
- [HOLMAN_ETL_PO_DETAILS gaps & consumer bugs](holman-etl-po-details.md) — PO_STATUS has no 'Open' (open=APPROVED); 5-day loader window leaks POs permanently; repair-start lookup dead (status filter + padStart key mismatch).
- [Prod executeSql swallows errors](prod-executesql-error-swallowing.md) — output "START TRANSACTION/ROLLBACK" = SQL error (bad column), NOT zero rows; header-only = zero rows; verify columns in schema files first.
- [Holman headless chromium in prod](holman-headless-chromium-prod.md) — prod ships only replit.nix nix deps; ungoogled's privacy patches broke the in-page TabId harvest (page.evaluate itself is fine, core is 1.41.2); fix = stock pkgs.chromium + unanchored resolver regex excluding ungoogled; can't install dotted playwright-driver.browsers nor edit replit.nix directly.
- [BYOV prefix check pad order](byov-prefix-pad-order.md) — zero-padding a truck number to 6 digits BEFORE checking the '88' BYOV prefix breaks 5-digit BYOV trucks (88144→088144); check prefix on raw/trimmed number first.
- [SMS quiet-hours deferral pitfalls](quiet-hours-deferral.md) — sign-inverted local→UTC made every quiet-hours deferral past-due (sent immediately, silent no-op); force/run-now routes must default dry-run + explicit confirm.
- [db:push blocked by interactive drift](dbpush-interactive-drift.md) — drizzle push hangs on a TTY-only external_apps_name_unique prompt; apply columns via raw SQL on dev (schema.ts kept in sync), never --force (truncate risk).
- [Upsert-only mirror sweeps](upsert-only-mirror-sweeps.md) — a ghost sweep on one Snowflake mirror doesn't fix sibling pages; every upsert-only sync table needs its own dropped_from_source_at sweep.
- [TPMS district mirror staleness](tpms-district-mirror-staleness.md) — truck-driven refresh never re-queries district-only transfers, so tpms_tech_profiles district goes stale; district decisions must live-check TPMS + heal the mirror.
- [Assign target validation](assign-target-validation.md) — every assign entry point must run server-side truck validation (format/exists/status/lock); UI-only guards let "byov" diverge TPMS/Holman.
- [Holman write-fence supersede](holman-write-fence-supersede.md) — fences outlive their correction when a newer op lands; release only on live-confirmed newer submissions, NEVER on cache confirms (fence pins the cache = circular evidence).
- [Spares pool & INTERIOR source](spares-pool-interior.md) — pool excludes assigned/BYOV/AMS-disposal vans (validate disposal via bulk VIN map only — lean caches lie); rack INTERIOR only from fs_all_vehicles_mirror.
- [Mismatch cache cold-start pinning](mismatch-cache-cold-start.md) — TTL caches over synced mirrors pin stale results on autoscale cold boots; invalidate via a shared epoch bumped after every mirror rewrite.
- [Samsara /devices health](samsara-devices-health.md) — includeHealth=true rejects limit=512 (use 100 + cursor); 2 devices/vehicle (VG gateway + CM camera); roll up worst-of per canonical truck number.
- [Holman phantom cache rows](holman-phantom-cache-rows.md) — two phantom classes (failed assign, optimistic create); only shared guard is last_holman_sync_at IS NULL — data_source and number format both lie.
- [Holman renewalDate field](holman-renewal-date-field.md) — Holman renamed reg-renewal to `renewalDate` (ISO); legacy names map to empty. Registrations tab date precedence: spares → fs_trucks → holman cache (full-fleet coverage).
- [fs_call_logs status vocab collision](fs-call-logs-status-vocab-collision.md) — RESOLVED: boot heal enforces lifecycle-only status; LUCA is sole caller (FS dial surfaces removed); new outcomes → both mapper maps + queue color map.
- [VRM rentals authority direction](vrm-authority-direction.md) — user directive: VRM owns rental state; port FS's good features INTO VRM; FS is a one-way downstream mirror, never the origin; lock FS manual rental-state edits.
- [Manual-override pins are episode-scoped](manual-override-expiry.md) — locks over scraped fields expire via board lifecycle clocks + dual-clock grace; reset reuses the scraper's pick; sweep UPDATEs must re-check predicates in-write, audit gated on rowCount.
- [CRLF files break exact-match edits](crlf-files-break-edit.md) — some committed files are CRLF (rightsize classifier + tests); verbatim edits silently miss — check `git ls-files --eol`, patch via CRLF-preserving script.
- [Bedrock prod secret staleness](bedrock-prod-secret-staleness.md) — prod Bedrock 403 while dev works = stale deployment secret snapshot; republish to fix; check vrm_rightsize_events.verdict_source for LLM-path liveness.
- [tpms_tech_profiles tech_id NOT unique](tpms-techid-not-unique.md) — multiple techs share one tech_id; any read/write keyed on tech_id corrupts (name-stamping); always key on enterprise_id; heal via run-tpms-name-heal.ts.
- [Rightsize rate policy](rightsize-rate-policy.md) — rate OR vehicle OR SMS are independent compliance paths (rate-secured = right-sized even in a larger unit); rate talk proposes DONE/review; confirm policy pivots with Tyler before rewiring math.
- [TEST-prefix dark launches lie](dark-launch-test-prefix-false-green.md) — if the far side skips processing for TEST traffic it skips validation too; 201 ≠ accepted. Reconcile payload against the vendor field table before the first live send.
- [Standard Activities route-block dark launch](standard-activities-route-block.md) — TEST prefix ≠ no POST; live gate is an env flag (default off, ActivityType unconfirmed); 409 never re-fire; no cancel API → warn on supersede; record date BEFORE filing.
- [Queue triage lanes & red policy](queue-triage-lanes.md) — lanes aren't step-shaped (step 2=action); "ready" membership gates on luca/manual evidence; workBucket claim rule = parity contract; bucket-section boards, not filter strips; red reserved; needs_replacement reversal.
- [Todays-queue perf layering](todays-queue-perf-layering.md) — 30s queue cache + 5min SWR PO-context + 5s spare-pool timeout; every new queue-mutating route must bust the cache or the settle refetch reads stale.
- [Shop-of-record single source](shop-of-record-single-source.md) — every surface incl. boards shows the server-reconciled pick (portal fallback only when field absent); dispatch map = namespaced truck:/case: keys.
- [Ready = phone-confirmed only](ready-phone-confirmed-only.md) — closed PO/ERD is a "confirm with shop" signal, never ready; verify/research marks are shared append-only rows across all three VRM views.
- [Automated VRM status writes](vrm-automated-status-writes.md) — guarded compare-at-write append (humans win); asymmetric absence rules; set membership ≠ full guard — re-assert the caller's whole predicate via GuardConstraints.
- [Queue tech-SMS single lane](queue-tech-sms-lane.md) — any "text the tech" surface reuses the pickup-text endpoints (Fleet Comms opt-out/quiet-hours/threading); never raw Twilio; send-path phone = fs_comms_contacts only (no fs_trucks fallback; caller phone only if contact blank); 24h dedupe is opt-in.
- [VRM queue pre-mortem risks](vrm-queue-premortem-risks.md) — open risks at the Ops Queue merge: fleet-status append/reconcile lock gap + unclosed compensation, adopt skips vocab validation, UTC/ET anchor drift, P2 owner arbitration by table order.
- [Stale vite page kills React dispatch](vite-stale-page-react-dispatch.md) — handler on fiber, clicks bubble, nothing fires, no errors = hard-reload the dev page before debugging app code.
- [VRM exec-summary freshness](vrm-exec-summary-freshness.md) — freshness clock = completed scheduled_sync runs ONLY (partial imports must not advance it); view-triggered auto-sync needs the durable import_runs 30-min guard, not just in-memory; ingest busts summary cache via leaf module.
- [Comms live phone pin](comms-live-phone-pin.md) — live_tpms history rows pin a contact's phone vs the lagging snapshot until FILE_DATE strictly passes the ET pull day; all phone writers share the pin xact lock.
- [Comms send-status contract](comms-send-status-contract.md) — refusals persist NOTHING, branch on ALL statuses; policy: no trade-based send blocking (HVAC gate removed; filter campaign lists instead).
- [Bedrock reasoning eats maxTokens](bedrock-reasoning-budget.md) — tight maxTokens returns EMPTY text (not truncated) on big prompts; never read content[0], take the first block with .text.
- [Shop-from-comments LLM fallback](shop-llm-fallback.md) — LLM runs only when header pick is empty or superseded by a newer payment PO; same gates as scraped headers; evidence-hash cache; force = cap-exempt.
- [LUCA activity ledger semantics](luca-activity-ledger.md) — ledger logs first sightings + run heartbeats, not every poll; a quiet ledger with healthy heartbeats is working as designed.
- [SSO-gated UI verification](sso-gated-verification.md) — screenshots hit the SSO wall; mint a temp sessions row (server reads DATABASE_URL, NOT DEV_DATABASE_URL), curl the API, replay client predicates on real rows, then delete it.
- [VRM surface alignment](vrm-surface-alignment.md) — all case surfaces share ONE displayShopFor/attach + cleanPhone gate; fs fallback is display-only (never call_*/LUCA); conflicting canon phones drop the fallback.
- [Vendor never-shop changes](vendor-never-shop-changes.md) — new banned tokens need JS+SQL regex parity + stored vendor_type backfill; drawers treat reconciledShop null as "no pick", never raw-fallback.
- [Registration/tags card context](registration-tags-context.md) — open renewal case ≠ tag blocker; AMS declined/auction van → tag status irrelevant (suppress block); recency = max(updated,scraped); Holman rows under both number cols.
- [Ready→status self-heal](ready-status-self-heal.md) — edge-triggered ready writers strand conflicts; fix = level sweep on queue GET (guard-only, throttled+refund); Verify click now also sets Scheduling.
- [VRM board read caching](vrm-board-read-caching.md) — heavy board reads serve-stale + bg rebuild; mutation busts ride the queue/PO-context invalidators one-way; cold concurrent builds can trip PG statement timeout.
- [Partial boot migrations](partial-boot-migrations.md) — a transient Neon timeout aborts the sequential boot DDL chain, silently skipping later ALTERs; prod-500/dev-200 on the same input = schema drift, diff information_schema first.
- [Publish bundle vs broken symlinks](publish-bundle-broken-symlinks.md) — dangling links (killed-chromium Singleton* under .config/chromium) abort the bundle phase; find via `find . -xtype l`, delete, republish.
- [Publish liveness verification](publish-liveness-verification.md) — deploys/history.json entry ≠ promoted; /api route answering SPA HTML = route absent; boot-DDL tables missing in prod = module never booted there.
- [Rental request/ETD pre-mortem debts](rental-request-etd-premortem.md) — open gaps: no booking claim/lease or state predicates, non-atomic token consume (dup bookings), over-broad cron bearer, prod-dead syncs, survey↛request seam.
- [Drizzle wraps pg errors](drizzle-query-error-wrapping.md) — `Failed query:` wrapper hides the constraint name on e.cause; `e.message.includes("<index>")` race handlers are dead code → 500s. Walk the cause chain.
- [Drizzle sql-tag regex escaping](drizzle-sql-tag-regex-escaping.md) — `\D` inside a drizzle sql`` template cooks to literal `D`; write `\\D` or bind params; audit by running both variants against prod counts.
- [Pooled advisory locks don't single-flight](pooled-advisory-lock-not-single-flight.md) — pool-issued pg_try_advisory_lock is re-entrant across requests sharing a connection + unlock leaks; use one dedicated client or row-level CAS.
- [LUCA shop-contact intake loop](luca-shop-contact-intake.md) — the one inbound LUCA write; vendor-match 409, humans win under a FOR UPDATE re-check, accepted contacts ALWAYS locked (unlocked luca phones are feed-invisible).
- [Test-fixture deletion scope](test-fixture-deletion-scope.md) — Tyler's "delete test records" means PROD pollution only; dev synthetic rows are working fixtures. Capture full rows before any delete.
- [Create Vehicle gate discipline](vehicle-create-gate.md) — duplicate checks fail CLOSED, VIN gate runs regardless of targets, number+VIN held by one reservation row; allocator must scan TPMS + fleet mirrors.
- [Holman create submit evidence](holman-create-submit-evidence.md) — "submitted"=receipt, "captured"=acceptance; no counts = unconfirmed, never mirror a cache row; custom-query DOES work when statusCode is sent.
- [Rental survey send audit findings](rental-survey-send-audit.md) — no vendor filter, submitted≠blocked re-issue, send never re-checks book, LDAP-in-employee_id join drops, home_phone unread, prod schema-health = deploy-liveness oracle.
- [ETD token runner ops](etd-token-runner.md) — headless mint verified on-box (stock nix chromium); any resolved DSN makes the store mandatory pre-login (env -u to smoke-test creds); prod vrm_etd_token comes only from app-boot publish.
- [External-op authorization fencing](external-op-authorization-fence.md) — atomic gate+insert; one-open index; parent-first finalize; serialized monotonic recovery; kill-switch at filing boundary; bearer-only evidence routes.
- [Create Vehicle wizard mirrors the gate](vehicle-create-wizard-gate-mirror.md) — one verdict drives button+text (two sources = the VIN-warning-that-never-blocked bug); failed check = warn not block; hold has a clock; pending ≠ success, recover by read-back never re-submit.
- [Request booking vs cutover split](request-booking-vs-cutover.md) — request workflow never files a route block (cutover-only); born not_applicable, completes on verified reservation; branch intent surfaces on workflow_type.
- [Cutover prod→dev pull](rental-cutover-prod-pull.md) — additive-upsert script only; intents tables are dev-only; copiers must skip GENERATED cols; refreshDevFromProd would wipe the fixtures.
- [ETD dual-runner parity](etd-dual-runner-parity.md) — one ledger, so hash+lock key+evidence spellings must match byte-for-byte; savedr refusals are HTTP-200 view models; synthetic LDAPs can't book.
- [ETD journey search is not an answer](etd-journey-search-identity.md) — Last30Days returns every QUOTE ever taken; a row counts only on positive ID (SHS ref/known conf), never "return all rows"; LDAP identifies a tech, not an intent.
- [Cutover arming semantics](cutover-arming-semantics.md) — armed flag ⇒ live default + dark-RBAC stand-down (runner routes stay cron-bearer); prod-only, dev-dark is test-enforced; armed ≠ autonomous (runner/sweep are operator-run).
- [Holman InboundVehicle write schema](holman-inbound-vehicle-schema.md) — statusCode is read-only (derived from dates); OOS submit = outOfServiceDate only; sending statusCode → HTTP 400 on /vehicles/submit.
- [Holman nightly batch windows](holman-nightly-batch-window.md) — 202 = queued; ~94% of changes apply at ~00:xx/~05:xx UTC, so verify via lastChangeDate and give lifecycle writes an action-aware timeout.
- [OOS exclusion on available-truck surfaces](holman-oos-cache-lag.md) — cache statusCode lags at 1 after an OOS submit while outOfServiceDate is already past; availability filters must use the durable signal (code 2 OR past date), never statusCode alone.
- [Route-block payload contract](route-block-payload-contract.md) — "Anytime" lets the optimizer move a time already texted to the tech (11/136 held 08:00); LocationValue = reserved branch ZIP5 or don't file; ActivityType 46 = "Vehicle - Change", confirmed.
- [/api/fs session ≠ authorization](fs-router-auth-gap.md) — the router only proves login (+ a cron-header bypass); operational modules must gate their own routes by role, and a dev-only sidebar link is not a gate.
- [CAS claims need crash recovery](cas-claim-crash-recovery.md) — a `pending` claim over an external side effect strands the row forever unless it is timestamped and recovered evidence-first (adopt what landed, release only when nothing did).
- [AMS bulk list contains sold history](ams-population-saledate.md) — live fleet = bulk rows WITHOUT SaleDate (~2.4k of ~11k); unsold auction/declined trucks stay; partial walks never clobber a complete population.
- [AMS in-repair read source](ams-vehicle-inrepair-read.md) — GET /api/v1/vehicles/{VIN} returns VehicleInRepair + TruckStatus together; the client's repair surface is otherwise write-only, so it reads as unqueryable.
- [Publish proposes DROP for boot-DDL tables](publish-drops-boot-ddl-tables.md) — publish diffs dev DB vs prod DB (tablesFilter does NOT protect fs_*); a dev app still on pre-merge code looks like "tables removed" → restart dev before publishing.
- [AMS bulk VehicleInRepair capture](ams-bulk-inrepair-flag.md) — bulk rows DO carry the flag (tri-state cache map); label-only "In Repair" under-excludes ~700 trucks; fail closed+visible on unknown AMS facts; only an active build may say "warming".
- [Odometer cycle & dry-run semantics](odometer-cycle-dry-run-semantics.md) — seed-never-fire on first sight, forward-only watermark, and NEVER stamp sent_at on a dry run or arming the gate goes silently dead.
- [Holman harvest 401 intermittent](holman-harvest-401-intermittent.md) — prod TabId-401 with valid creds/binary = portal-side transient; triage via holman_po_sync_meta + cron-refresh curl, not (often-empty) deploy logs.
- [Rental extension request semantics](rental-extension-requests.md) — extension = more time on same unit, always pending, approved is terminal; FOUR booking doors (lease, /book, /booked, createIntent) must all exclude it; booked new doesn't block an extension.
- [Dev comms send LIVE SMS](dev-comms-live-sms.md) — COMMS_SEND_LIVE is on in dev; decide/notify routes text real technicians during testing — verify state via SQL, not the route.
- [Approved pickup time → ETD booking](request-pickup-time-flow.md) — quote-feeding request-row edits must invalidateRequestPreviews (commit books the stored preview); clear etd_error on accepted retry; preview_failed is a code, not a status.
- [Blocked-since clocks](blocked-since-clocks.md) — re-evaluated-every-sweep states need their own since column preserved through clear→re-mark round trips; "complete set" surfaces need an uncapped read, never the capped list.
- [Enterprise direct-billing import](direct-billing-import.md) — ExcelJS reads this vendor xlsx as ZERO sheets (raw-OOXML jszip parse); full-state upload must refuse layout drift or the sweep mass-drops; truck = live TPMS only; PO-less source excluded from override expiry.
- [jsdom component-test recipe](jsdom-component-test-recipe.md) — render real client pages under node:test via tsx --tsconfig tsconfig.dom-tests.json (jsx react-jsx), generic window-constructor copy, fetch route table, toast probe.
- [--test-force-exit truncates suites](node-test-force-exit-truncation.md) — the flag can silently drop whole describe blocks (green run, lower test count); use it only for suites holding real handles.
- [Schedule lookups from UI lanes](approval-drawer-schedule-latency.md) — fetchScheduleWindow cold-start can take ~60s from a drawer route; keep schedule facts advisory, never block a human action on them.
- [Rental extension reminders](rental-extension-reminders.md) — cycle key = days_authorized rarely moves → one text per case in practice; arming fires the whole overdue backlog; dry runs never consume the cycle slot; standalone sweeps must ensure dependency schemas.
- [Radix Select async-prefill wipe](radix-select-async-prefill-wipe.md) — bubble-input coerces a controlled value through a native select; setting a value before its async options load fires change("") and erases it (real browsers too).
- [Double-billing detection premortem](double-billing-premortem.md) — ranked open risks: comparison failure reads as clean, unmeasured coverage gaps, unknown≠clean, write-once stamp has no void, schema-health misses new columns.
- [Rental drawer status consolidation](rental-drawer-status-consolidation.md) — ONE shared derivation feeds drawer card + list badge; hideStatus panel (default false); raw errors only in the expander; flat scroll — Fleet vetoed collapsible sections; class picker = fixed Enterprise menu, never free text.
- [Today's Queue offline test harness](todays-queue-offline-test-harness.md) — buildTodaysQueue runs hermetically by patching getAllTrucks + db/fsDb.execute (PgDialect text dispatch) + db.select-throw; mutation-verify contract tests.
- [Samsara Snowflake table quirks](samsara-snowflake-tables.md) — MAINT_ID = vehicle id (no VEHICLE_ID in maintenance/odometer); STREAM has LATITUDE/LONGITUDE not LAT/LNG; sibling event tables audited OK; probe real columns first.
- [Public-form screenshot / SOP recipe](form-screenshot-sop-recipe.md) — never submit (live SMS), pick LDAPs via the identity-resolution join, mask PII in-DOM, target Radix selects by placeholder text.
- [Cutover billing-switched evidence](cutover-billing-switched-evidence.md) — effective = live direct-book presence OR stamp; void wins over book evidence; import-time-only stamps go dead when prod imports predate the code.
- [Viewport-fit UI guards](viewport-fit-guard.md) — assert pane-bottom window at 2 sizes (one size can be pixel-identical); shared lib scripts/lib/viewport-guard.ts; bind shells h-screen+min-w-0, never clamp tall-header pages.
- [Off-page direct-billing list scope](direct-offpage-scope.md) — only a BOOKED cutover row excludes; old-book test is identity-based (no anchors exist, no 'rolled'); unresolved = 'unknown' bucket, never clean.
- [Cutover Holman-book anchoring](cutover-book-anchoring.md) — enterprise_direct rows share vendor 'Enterprise Rent-A-Car' with the ECARS book (filter source='enterprise'); case rows overwrite per truck → book state rides booking-time ticket anchors, never force-overwrite non-empty ones.
- [Renter assigned-truck single source](renter-assigned-truck-source.md) — TPMS-first shared fragment only; roster-only truck_lu copies go stale and fed LUCA wrong trucks + starved redirect PO/scrape coverage.
- [VRM page parity audit map](vrm-page-parity-audit.md) — surfaces composing master-board derivations are aligned; drift re-enters via independent copies (repair tracker, Holman PO queue, legacy /rental-operations FS-mirror page).
- [Holman new-process denial](holman-new-process-denial.md) — ALL Holman-queue arrivals (new+extension) denied w/ redirect SMS branched on shared cutover-standing predicate; post-external-write guards must degrade, never abort logging/SMS.
- [etd-executor test baseline is red](etd-executor-test-baseline.md) — 5 stale failures + a flaky claim-limit test on a clean tree; diff failing NAMES vs a fresh baseline run, never trust the fail count.
- [Tech shifts schedule feed](tech-shifts-feed.md) — secret name is plural TECHS_SHIFTS_API_KEY (client takes both); fetchShiftRows caches by query only (env-blind — test trap); VRM page + Fleet Scope wrapper + request drawer.
