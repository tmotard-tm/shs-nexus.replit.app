- [LOA Recovery department scoping](loa-recovery-scoping.md) — agent visibility rides on department/queue access (no flag); cross-queue strip must filter to accessible modules; LOA `department` labels need substring→module mapping, not `departmentToQueueModule`.
- [LOA queue-item data shape](loa-queue-item-shape.md) — LOA items use a flat data shape (no `data.source`/`technician` wrapper); Assets queue UI silently drops them unless `isItemFromSync()` + `parseTechData()` are taught the shape.
- [Assets Queue truck-number candidate fallback](assets-queue-truck-candidate-fallback.md) — nexus-data join needs multiple truck-number sources per item (incl. rosterContact.lastKnownTruckLu); distinguish "no candidate" vs "no matching row" via getNexusMatchStatus().
- [LOA Recovery duplicate prevention](loa-recovery-duplicates.md) — startup+scheduler race doubled cases; durable guard is a partial unique index on open `(workflow_id,department)` + ON CONFLICT DO NOTHING, not just a mutex; Fleet lane column='Fleet Management' but data.lane='FLEET'.
- [LOA SOP timeline & recovery signal](loa-sop-recovery-signal.md) — recovery-initiated = real Fleet sibling (no self-status fallback); paused = real recoveryPaused flag (not date guess); outreach steps never claim "Sent"; cross-lane feed must be queue-access gated.
- [Autoscale listen-first](autoscale-listen-first.md) — deploy promote fails ("port never opened, expected 5000") if heavy DB/Snowflake bootstrap is awaited before server.listen(); defer it to a post-listen runStartupBootstrap().
- [VIN auto-decode on Create Vehicle](vin-decode-create-vehicle.md) — NHTSA vPIC fills Year/Make/Model/AssetType; AssetType is a constrained-6 heuristic that returns "" when uncertain; spinner uses a seq ref, not a cancelled flag.
- [Vehicle district update](vehicle-district-update.md) — per-vehicle district fan-out to TPMS/WMS/Holman; Holman prefix=last-4 on UPDATE (not create-flow's full district); only mirror cache when all 3 succeed.
- [BYOV/vehicle number reservation](byov-number-reservation.md) — create reserves number via active-row + partial-unique-index before fan-out; VIN discriminates retry vs collision; stale reclaim MUST be CAS, not blind id update.
- [Neon HTTP driver boolean pitfall](neon-http-driver-boolean-pitfall.md) — one-shot neon() HTTP queries mis-read booleans; use the app's pool driver (server/db.ts) for DB ground truth.
- [communication_logs.sent_by FK trap](communication-logs-sentby-fk.md) — system sends must pass sentBy=null (actor→metadata.sender); a non-user string throws after the email sent, silently losing the log + *_sent_at → daily re-fire.
- [Holman 202 ≠ applied (assign + district)](holman-assignment-verification.md) — 202/errorCount-0 = "queued", not applied; trust post-sync cache. Same driver failing repeatedly (UNKNOWN) while others succeed on the same vehicle = Holman-side record issue, not a Nexus bug. District change is verified async via fleet sync; sync FREEZES cache.district so verifyFromFleetData is the only authoritative completer — never optimistic-mirror district.
- [Startup init kills route registration](startup-route-registration.md) — an unbounded `await` between app.get/post in registerRoutes 404s every later route group; detach optional init. Startup DDL FK'ing a discovery-written table hangs on autoscale — skip-if-exists + DB lock_timeout/statement_timeout, not JS timeouts.
- [Holman district payload traps](holman-district-payload.md) — district submit must OMIT assignedStatusCode (string(1)); stored holman_submissions.payload has tracking-only targetPrefix/targetDistrict that must be stripped before any replay; 202≠accepted.
- [Neon WS drop → empty-message 500](neon-ws-drop-empty-message-500.md) — heavy Neon aggregator routes 500 {"message":""} on transient WS drop (closeCode 1006, empty ErrorEvent.message); serve bounded-stale cache only for transient drops.
- [Partial refresh must never clobber last-good cache](partial-refresh-clobber.md) — incomplete sweep results keep/rehydrate complete data + short retry deadline, never overwrite for the full TTL; self-fetching localhost routes 401 in prod.
- [AMS scorecard zeros vs pending](ams-scorecard-pending-state.md) — fixed-bucket-list + `?? 0` defaults render a misleading all-zeros card on 503/cold sweep; gate display on data&&activeSweepComplete, retry(5xx-only)+poll-until-complete, AbortController-cancel slow AMS pages.
- [TPMS /techinfo accepts a truck number](tpms-techinfo-truck-lookup.md) — GET /techinfo/{id} returns the assigned tech for a TRUCK number too, not only Enterprise ID; code comment/string claiming "no truck lookup" is WRONG (Postman example /techinfo/023132); truck→tech need not be cache-only.
- [AIMS authority semantics](aims-authority-semantics.md) — OWNERLDAPID (not LDAPID, which disagrees ~60%) is the assign authority; confirm by truck# not owner-ldap (termed owner 404s); TPMS "not assigned" = HTTP 400 "No Data Found".
- [techsupdatedafter blind to assignments](tpms-techsupdatedafter-assignment-blindness.md) — feed tracks profile/roster edits only; truck assign/unassign (UPDATE_TRUCK_OWNER→AIMDAO) never appears; reconcile assignments via live GET /techinfo (or Snowflake TRUCK_LU), not the feed.
- [TPMS↔Holman number matching](tpms-holman-number-matching.md) — TPMS pads truck #s, Holman doesn't; always match canonical on BOTH sides; vehicle_nexus_data legacy dup-format rows → read via variants, dedupe canonical.
- [Fleet reconciliation dry-run](fleet-reconciliation-dryrun.md) — tier-3 backstop live facts: AMS paginate via envelope `total` (cap 1000); Holman vehicle# is `holmanVehicleNumber` not `clientVehicleNumber`; baseline vs live (AMS ghost ~892 live, Holman lifecycle ~93 scoped).
- [Fleet reconciliation backfill end-state](fleet-reconciliation-backfill-endstate.md) — post-drain mix is NOT all-verified; held/skipped/verification_pending are correct-by-design (authority-contested, CAS-drift, async sync), not failures; applying self-heals after 10-min lease; console workflows can auto-restart.
- [Holman PO refresh liveness](holman-po-refresh-liveness.md) — listing GET is live per-request; KPI token is TEMPLATE-stable (same across logins ≠ frozen); API-postback approvals linger on the grid ~30–85 min, manual portal approvals clear <6 min.
- [Holman PO reopen guard](holman-po-reopen-guard.md) — rental extensions re-authorize the SAME PO# at the same $0.00; amount-only reopen hid them forever; fix = grace/resubmit reopen + grid_last_seen_at sighting stamp.
- [Holman portal prod risks](holman-portal-prod-risk.md) — VRM rental-PO: headless Chromium (binary may be absent in autoscale image), live writes armed by inverted HOLMAN_DECISION_DRY_RUN (a global Secret → applies to prod too, not per-env), access gated to a VRM-local username allowlist (jmorga1/handers), not RBAC.
- [Fleet Ops pre-mortem risks](fleet-ops-premortem-risks.md) — non-obvious fleet failure modes: write-through divergence (downstream applied, local failed), assign/unassign SWALLOWS Holman submit-persist failure (district surfaces it), no idempotency key, sync clobbers tech fields (only district frozen), frozen-district deadlock.
- [Long commands & typecheck in Replit env](replit-long-commands-typecheck.md) — bash bg procs die on tool-call return; run full tsc (`npm run check`, NOT `typecheck`) via a workflow + poll logfile; LSP only sees open files; verify your files add ZERO new errors vs the ~224 baseline.
- [Architect review result-buffer truncation](architect-review-buffer.md) — architect() echoes relevantFiles+gitDiff into a ~35KB result; large files truncate the verdict. Review changes via includeGitDiff:true + empty relevantFiles + inlined schema facts; read result tail.
- [tpms_cached_assignments phased retirement](tpms-cached-assignments-retirement.md) — reads on tpms_tech_profiles; writes now FROZEN (FREEZE_TPMS_CACHE_WRITES); don't drop schema until legacy readers verified. Truck→tech = canonical ltrim-zeros match, most-recent wins.
- [AIMS/Snowflake authority](aims-snowflake-authority.md) — AIMS_TRUCK_INFO is historical (filter max FILE_DATE→DELIND=0 for active); TEST_DB default inert (fully-qualify); ext systems = single shared prod instances; date header=UTC≠ET.
- [All Vehicles roster mirror](all-vehicles-mirror.md) — /api/fs/all-vehicles serves 4 Snowflake sources from a daily PG mirror to kill the Neon WS 503; mirror MUST be a raw transport cache (no dedupe, unassigned kept raw) — byte-for-byte parity beats a normalized schema.
- [Snowflake sync lock](snowflake-sync-lock.md) — ONE advisory lock ('fleetscope-mirror-sync') serializes ALL heavy Snowflake roster reads (mirror/TPMS/AMS); new readers must join it, wrap only the read, never nest.
- [Security-questions fail-open](security-questions-fail-open.md) — client must NOT force the SQ prompt on a transient status-check failure; failing closed caused random re-prompts for already-enrolled users.
- [dry_run holds active-target lock](dry-run-active-target-lock.md) — materialized dry_run items (status=queued) reserve the recon {system,truck,field} lock and silently block real canary/nightly/backfill writes; give them a non-active status.
- [WMS cost-center update safety](wms-cost-center-update-safety.md) — getTruck/getAllTrucks expose only a 13-field projection (no locationId/isActive); derive identity from truck#; updateTruck merges (only costCenter changes), non-projected NetSuite fields are unobservable+irrelevant.
- [Reconciliation auto-apply gate](reconciliation-auto-apply-gate.md) — Automate toggle must stay fail-safe OFF; Apply = real Holman/WMS/AMS writes; never auto-apply on materialize halt/startup; app_settings absence = OFF, not crash.
- [tpms_tech_profiles.truckNo refresh sources](tpms-truckno-refresh-sources.md) — authoritative truck writer is the 7:30 AM LIVE per-tech TPMS call, NOT the nightly TPMS_EXTRACT snapshot (which lags post-midnight moves ~24h); feed is move-blind → don't write truckNo from it, and don't source truckNo from the snapshot.
- [District cost-center auto-seed disabled](district-cost-center-autoseed.md) — keep DISTRICT_COST_CENTER_AUTO_SEED_ENABLED=false; auto-seed has no denylist so it resurrects admin-deleted/dirty districts (3132/3580). Manual-only via force trigger.
- [AMS Current Location source](ams-current-location-source.md) — fleet-card CurLoc must come from full-fleet searchVehicles cache (batchFetchAmsCurrentLocation), NOT ams_vehicles_cache.rawResponse (tech-shaped, no CurLoc); chain: full-fleet→DB-cache→Holman, never delivery addr.
- [AMS truck-status-map client resilience](ams-truck-status-map-client-resilience.md) — endpoint 503s by design while AMS cache warms; shared client query MUST retry(5xx/network)+poll, never retry:false, or all card pills/labels blank until manual refresh.
- [AMS user-updates write contract](ams-user-updates-contract.md) — POST user-updates validates enum fields (color/truckStatus/…) by NAME not UniqueID; Edit dialog must id→label convert + send only changed fields (re-sent stale color id 500'd "Invalid color").
- [Rental Ops → Fleet Scope sync](rental-ops-fleet-scope-sync.md) — destructive fs_trucks consolidate needs a platform Scheduled Deployment (autoscale kills in-process timers); watermark = completed sync_logs row type 'rental_ops_fleet_scope', NOT fs_rental_imports (manual weekly path).
- [App Launcher icons are DB rows](app-launcher-icons.md) — external_apps.logo_url not code; dev/prod separate DBs; insert-only seed won't overwrite on republish → value-guarded self-heal UPDATE fixes prod (prod DB is read-only via tools).
- [Comms module data restore after merge](comms-module-restore-after-merge.md) — merging the comms side-task brings code, not its fs_comms_* rows; re-run cutover per DB in order sync→migrate→sync (2nd sync's enrich+auto-archive keys unmatched to original tech & archives non-current). Orphan tsx proc → port 5000 EADDRINUSE.
- [Comms dev/prod split-brain](comms-dev-prod-split-brain.md) — "previews but empty threads" = check msgs_linked; interrupted dev-from-prod refresh strands messages; heal script snapshots fs_comms_* atomically + neutralizes copied send queue.
- [LOA outreach 2-day cadence cap](loa-outreach-cadence-cap.md) — max 2 distinct send days per cycle; reply/form/cap stop permanently, staff re-enable resets counter; backfill must be column-add-guarded or restarts re-cap re-enabled techs.
- [Master Fleet Communications Module](master-fleet-comms.md) — LDAP-keyed team-SMS inbox (fs_comms_*); dark-rollout flag, autoscale schedulers, WS payload.type override, contacts-sync ts trap; inbox rental-flag + bulk join by LDAP (not truck#).
- [Prod sync schedule reality](prod-sync-schedule-reality.md) — in-process sync-scheduler timers barely fire on autoscale (watermark polls NEVER ran in prod); audit via sync_logs + max(updated_at), not the coded schedule.
- [TPMS tech-specialty tier-3 snapshot](tpms-tech-specialty-snapshot.md) — TPMS_EXTRACT_LAST_ASSIGNED = last tech EVER on truck (never clears); tier-3 fallback shows it as current assignee → ghost assignments; live-verify via /techinfo/{truckNo} first.
- [OpenAI keys degrade silently](openai-keys-fail-soft.md) — both OpenAI features are fail-soft; out-of-quota keys pass /v1/models but 429 on completions, so "broken AI summaries" = check billing first, not code.
- [Batch outbound-call safety](batch-outbound-call-safety.md) — never await a phone-call batch in the handler (proxy kill → retry → double-dial); real fix = DB dedup guard incl. timeout-ambiguous dials logged guard-visible.
- [Dev server port contention](dev-server-port-contention.md) — "didn't open port 5000" despite serving = duplicate `npm run dev` from a Shell/Codex session; check proc parent chain + orphans before retrying restarts.
- [HOLMAN_ETL_PO_DETAILS gaps & consumer bugs](holman-etl-po-details.md) — PO_STATUS has no 'Open' (open=APPROVED); 5-day loader window leaks POs permanently; repair-start lookup dead (status filter + padStart key mismatch).
- [Prod executeSql swallows errors](prod-executesql-error-swallowing.md) — output "START TRANSACTION/ROLLBACK" = SQL error (bad column), NOT zero rows; header-only = zero rows; verify columns in schema files first.
- [Holman headless chromium in prod](holman-headless-chromium-prod.md) — prod ships only replit.nix nix deps; ungoogled's privacy patches broke the in-page TabId harvest (page.evaluate itself is fine, core is 1.41.2); fix = stock pkgs.chromium + unanchored resolver regex excluding ungoogled; can't install dotted playwright-driver.browsers nor edit replit.nix directly.
- [BYOV prefix check pad order](byov-prefix-pad-order.md) — zero-padding a truck number to 6 digits BEFORE checking the '88' BYOV prefix breaks 5-digit BYOV trucks (88144→088144); check prefix on raw/trimmed number first.
- [SMS quiet-hours deferral pitfalls](quiet-hours-deferral.md) — sign-inverted local→UTC made every quiet-hours deferral past-due (sent immediately, silent no-op); force/run-now routes must default dry-run + explicit confirm.
- [db:push blocked by interactive drift](dbpush-interactive-drift.md) — drizzle push hangs on a TTY-only external_apps_name_unique prompt; apply columns via raw SQL on dev (schema.ts kept in sync), never --force (truncate risk).
- [Upsert-only mirror sweeps](upsert-only-mirror-sweeps.md) — a ghost sweep on one Snowflake mirror doesn't fix sibling pages; every upsert-only sync table needs its own dropped_from_source_at sweep.
- [TPMS district mirror staleness](tpms-district-mirror-staleness.md) — truck-driven refresh never re-queries district-only transfers, so tpms_tech_profiles district goes stale; district decisions must live-check TPMS + heal the mirror.
- [Assign target validation](assign-target-validation.md) — every assign entry point must run server-side truck validation (format/exists/status/lock); UI-only guards let "byov" diverge TPMS/Holman.
- [Holman write-fence supersede](holman-write-fence-supersede.md) — fences outlive their correction when a newer op lands; release only on live-confirmed newer submissions, NEVER on cache confirms (fence pins the cache = circular evidence).
- [Spares pool & INTERIOR source](spares-pool-interior.md) — unassigned pool derived locally (holman cache minus tpms occupied, BYOV excluded, Snowflake fallback guards); rack INTERIOR only in fs_all_vehicles_mirror — holman cache interior is a 'Standard' default.
- [Mismatch cache cold-start pinning](mismatch-cache-cold-start.md) — TTL caches over synced mirrors pin stale results on autoscale cold boots; invalidate via a shared epoch bumped after every mirror rewrite.
- [Samsara /devices health](samsara-devices-health.md) — includeHealth=true rejects limit=512 (use 100 + cursor); 2 devices/vehicle (VG gateway + CM camera); roll up worst-of per canonical truck number.
- [Holman phantom cache rows](holman-phantom-cache-rows.md) — upsert-only sync never removes failed-assign manual rows; self-heal keys on manual+never-synced, NOT number format (real Holman numbers can be alphanumeric).
- [fs_call_logs status vocab collision](fs-call-logs-status-vocab-collision.md) — LUCA writes display labels into the lifecycle column; queue "unresolved" + follow-up 'completed' filters misread every LUCA row; new outcomes must update BOTH mapper outcome maps.
- [Manual-override pins are episode-scoped](manual-override-expiry.md) — locks over scraped fields expire via board lifecycle clocks + dual-clock grace; reset reuses the scraper's pick; sweep UPDATEs must re-check predicates in-write, audit gated on rowCount.
- [CRLF files break exact-match edits](crlf-files-break-edit.md) — some committed files are CRLF (rightsize classifier + tests); verbatim edits silently miss — check `git ls-files --eol`, patch via CRLF-preserving script.
- [Bedrock prod secret staleness](bedrock-prod-secret-staleness.md) — prod Bedrock 403 while dev works = stale deployment secret snapshot; republish to fix; check vrm_rightsize_events.verdict_source for LLM-path liveness.
- [Rightsize rate policy](rightsize-rate-policy.md) — rate OR vehicle OR SMS are independent compliance paths (rate-secured = right-sized even in a larger unit); rate talk proposes DONE/review; confirm policy pivots with Tyler before rewiring math.
