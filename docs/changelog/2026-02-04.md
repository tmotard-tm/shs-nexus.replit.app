# Changelog - 2026-02-04

## Sprint 9: Tools Recovery Queue Redesign

### Major Changes

#### UI/UX Overhaul
- **Table-Based Layout**: Replaced card-based tabs with a modern table featuring sortable columns
  - Columns: Technician, District, Sep Date, Vehicle Type, Routing, Status, Tasks Progress
- **Expandable Rows**: Inline row expansion replaces separate detail view navigation
  - 3-column expansion panel: Contact Details, Recovery Tasks, Quick Actions
- **Header Stats**: Total cases, urgent count, active count, done count

#### Enhanced Filtering System
- Search by technician name or enterprise ID
- Multi-select filters: Status, Vehicle Type, District
- "Incomplete Only" toggle to focus on outstanding items
- Pagination with 10 items per page

#### Urgency Matrix System
- Vehicle Type + Days Until Separation determines urgency level:
  - **CRITICAL**: Rental ≤7 days OR BYOV ≤2 days
  - **HIGH**: BYOV >2 days OR Company ≤2 days
  - **STANDARD**: Company >2 days
- Visual badges (red/orange/gray) indicate urgency throughout

### Backend Changes
- Enhanced `GET /api/tools-queue` to enrich items with technician data from `all_techs` table
- Returns: district, contact info (phones, email), separation date
- Added `techData` object to each queue item response

### Schema Changes
- Added `vehicleType` text column to `queue_items` table
  - Values: 'company' | 'byov' | 'rental'

### Files Modified
- `client/src/components/tools-queue/ToolsRecoveryQueue.tsx` (new component)
- `client/src/pages/tools-queue.tsx` (simplified to use new component)
- `server/routes.ts` (enhanced tools-queue endpoint)
- `shared/schema.ts` (added vehicleType column)

### Preserved Functionality
- Auto-save with 500ms debounce via `useDebouncedSave` hook
- Task checkboxes, carrier dropdown, routing radio buttons
- Assign/complete workflows
- External links to Tool Audit Form and Segno

---

## Sprint 8: Auto-Save for Task Progress

### Backend
- Added `PATCH /api/tools-queue/:id/save-progress` endpoint for partial updates
- Accepts: task booleans, carrier, fleetRoutingDecision
- Added `updateToolsQueueProgress()` method to storage interface

### Frontend
- Created `client/src/hooks/use-debounced-save.ts` hook with 500ms debounce
- Optimistic UI updates with save status indicator (Saving.../Saved/Error)
- Flush-on-unmount using navigator.sendBeacon to prevent lost updates
- Mark Complete button shows warning dialog if tasks are incomplete

---

## Sprint 7: Enhanced Tools Task Detail View

### New ToolsTaskDetailView Component
- 3-column layout: Contact & Routing, Task Checklist, Actions
- Contact info from all_techs table, vehicle location from Samsara GPS
- 6 interactive checkboxes + carrier dropdown, routing radio buttons
- External links: Tool Audit Form, View in Segno

---

## Sprint 6: Schema Extension + Contact Info Mapping

### Task Checklist Schema Extension
- Added 7 new columns to queue_items:
  - task_tools_return, task_iphone_return, task_disconnected_line
  - task_disconnected_mpayment, task_close_segno_orders, task_create_shipping_label
  - carrier

### Contact Info Endpoint
- Added `GET /api/tools-queue/:id/contact` to fetch technician contact info

---

## Commits Today
- `0658514` - Add technician data to the tools queue for better process visibility
- `1231963` - Add functionality for users to assign tasks to themselves
- `e7edfe3` - Improve user experience by preventing duplicate clicks on task cards
- `f3cb6ce` - Add warning for incomplete tasks before marking a workflow complete
- `3994056` - Add auto-save functionality for task progress and routing selections
- `3575a89` - Introduce a new enhanced task detail view for tools queue items
- `08d4433` - Add contact information retrieval and update workflow tracking fields
