# Changelog: 2026-02-05

## Sprint 11: Mobile Phone from TPMS + Source Indicator

### What We Built
1. **Mobile Phone Integration**
   - New `getMobilePhoneByLdap()` method in SnowflakeSyncService
   - Queries `PRD_TPMS.HSTECH.COMTTU_TECH_UN` for active techs with mobile phones
   - Uses ROW_NUMBER to select primary assignment when tech has multiple records
   - Replaced "Work Phone" with "Mobile Phone" throughout Tools Queue UI

2. **Snowflake Source Indicator**
   - Added `fromSnowflake` boolean flag to techData objects
   - Discrete "Source: Snowflake" text appears next to enterprise ID for HR-sourced items
   - Helps distinguish auto-populated items from manually created ones

### Files Modified
- `server/snowflake-sync-service.ts` - Added getMobilePhoneByLdap() method
- `server/routes.ts` - Updated /api/tools-queue and /api/tools-queue/:id/contact endpoints
- `client/src/components/tools-queue/ToolsRecoveryQueue.tsx` - Mobile phone label, Source badge
- `client/src/components/tools-queue/ToolsTaskDetailView.tsx` - Mobile phone in contact info
- `replit.md` - Updated session handoff

### Technical Details
- **HR Separation Filter**: Only shows techs where `LAST_DAY IS NOT NULL` (8 of 66 records)
- **Mobile Phone Query**: Filters on `TECH_STS_CD = 'A'` and `ACTIVE_IND = 'Y'`
- **Data Priority**: HR separation data > all_techs > queue item data

### What's Next
1. Add FleetScope deep link for routing lookup
2. Implement Phase 2 email notifications when Fleet tasks are created
3. Add Playwright E2E tests for expandable row interactions

---

## Sprint 0: Snowflake Separation Sync for Offboarding Automation

### What We Built
1. **New Separation Detection**
   - `getSeparationsSinceTimestamp()` method queries HR table for records created after last sync
   - Uses `CREATED_AT > ?` filter to detect only new entries
   - Returns full separation details including contact info, truck number, fleet pickup address

2. **Automatic Offboarding Task Creation**
   - `syncNewSeparations()` creates Day 0 tasks for Tools, Fleet, and Inventory queues
   - Includes technician info, vehicle details, and HR notes in task data
   - Deduplication: Skips if existing tasks found within 45-day window

3. **5-Minute Polling Scheduler**
   - Added `checkAndRunSeparationPoll()` to sync-scheduler.ts
   - Polls every 5 minutes for new separation records
   - Logs results and tracks last poll timestamp

4. **Manual Trigger Endpoint**
   - `POST /api/snowflake/sync/separations` for testing and manual sync
   - Developer/Admin role required

### Files Modified
- `server/snowflake-sync-service.ts` - Added getSeparationsSinceTimestamp(), syncNewSeparations()
- `server/sync-scheduler.ts` - Added 5-minute separation poll, triggerSeparationPoll()
- `server/routes.ts` - Added POST /api/snowflake/sync/separations endpoint

### Technical Details
- **HR Table**: `PRD_TECH_RECRUITMENT.FLEET_DETAILS.SEPARATION_FLEET_DETAILS`
- **Sync Log Type**: `separation_poll` (tracks last successful poll)
- **Default Lookback**: 24 hours if no previous sync log exists
- **Tasks Created**: 3 per separation (Tools, Fleet, Inventory)

### Acceptance Criteria Met
- ✅ Nexus can query Crystal's Snowflake separation table
- ✅ New separation records trigger offboarding task creation
- ✅ Sync runs automatically on schedule (every 5 minutes)
- ✅ Logs show successful syncs and any errors

### What's Next
1. Tool Audit email notification when separation syncs
2. Test with fresh HR data to verify task creation
3. Add monitoring dashboard for sync status
