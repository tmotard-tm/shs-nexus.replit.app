Plan #201 — Closeout Prompt (Steps 4 verification + 5, 6, 7, 8, and optional 9)
Stop and re-read this before editing any file. Step 4 was marked complete based on API verification, not UI verification. The deployed page still renders as a single flat table with the old Last Action column, "No data" in every Tech Punch Status cell, no section headers, no Stage pill, and no flag tints. That is not "Done looks like" passing. Start by proving Step 4 actually renders in the browser, fix it if it doesn't, then proceed through 5–8 in one pass. Do not mark any step Done until you have attached a screenshot of the live UI showing the acceptance bullet.
Step 4 — Visual verification & repair (required first)

Open the deployed tracker page (not the API) in a logged-in browser session. Take a full-page screenshot.
Verify all of the following are visually present. For each missing item, fix it in the client code and re-screenshot:

Three collapsible section headers with row counts: Action Needed (expanded), In Progress (expanded), Completed (collapsed by default).
A Stage pill column positioned immediately left of Shop Status, with color-coded values (Needs Tech Call / BYOV Decision / Awaiting Rental Return / Awaiting Route Clear / In Repair / Ready for Pickup / Complete).
Tech Punch Status cells showing real values (Punched In / Punched Out / Unknown) with the ⓘ tooltip on the column header.
Flag tints actually rendered on rows: red background for stuck-on-rental-return, yellow for stale shop contact, blue left-border for notify-routing. Hover tooltips describe the flag.
The Last Action column is gone from the visible table.
"Show archived (Completed >14 days)" toggle is visible and functional.


If any of the above is missing, the client refactor did not fully land. Diagnose (stale bundle, incomplete JSX, feature flag, routing fallback) and fix. Common culprit: the new section-rendering code was added but the original single-table render block was not removed, or the new code is behind a condition that's evaluating false.
Do not proceed to Step 5 until all six items above are verified with screenshots.

Column decluttering (apply during Step 4 verification)
Replace the current column set with this tightened layout. The current table is too wide and shows fields that belong in the drawer or a hover. Final visible columns, in order:

Case — two-line cell: Tech Name (bold) / LDAP (muted, small).
Truck # — clickable, opens AMS drawer. Subline: small muted AMS Odometer text. Status dot (green/gray/red) to the left.
Denied — two-line cell: Denied Date / Denial Reason (muted, small; blank when null).
Stage — colored pill (the new computed field).
Punch — Punched In / Punched Out / Unknown. Header has ⓘ tooltip about the heuristic.
Shop Status — main status pill with sub-status as a small second line (e.g., "Repairing / Under repair at shop").
Van Status — pill (includes Route Canceled rendering).
Shop — two-line cell: Shop Name (truncate to ~25 chars with full-text tooltip) / Shop Phone (muted, click-to-call). Shop ETA appears as a third line only when set.
Tech Outreach — latest-note preview (~45 chars) with author initials + relative date; click opens modal to Tech Outreach tab. Empty state: "— No outreach yet —" in muted italic.
Shop Log — same pattern as Tech Outreach, for Shop Contact Log.
BYOV — pill: Not Offered / Offered / Enrolled / Declined, with decision date as a small subline when set.
Rental Returned — Yes/No chip + date subline when Yes.
Route Cleared — Yes/No chip + date subline when Yes.
Flags — small icon column for the red/yellow/blue indicator with tooltip.
Actions — edit (pencil), close case (when Stage=Complete), reopen (when archived).

Move into the edit modal / drawer instead of showing as columns:

Tech Phone (into modal header; click-to-call from there)
District (into modal header, hover-revealed on row)
Supervisor + Supervisor Phone (into modal header, hover-revealed on row)
Assigned Tech Liaison / Shop Liaison (into modal; show initials badge only in row hover, not a full column)
Repair Shop address beyond name (into modal; Truck # drawer also shows it from AMS)

Retire entirely from the UI:

Last Action column (data stays read-only in DB, surfaced via Guardrail 1 below)
Tech Punch Status "No data" states — replace with the actual value or error string per Step 3

The compact column set keeps the page scannable at normal laptop width (~1440px) without horizontal scroll. If it still overflows after these changes, move BYOV, Rental Returned, and Route Cleared into a single composite "Handoff" column with three small chips stacked vertically.
Step 5 — Notes UX with legacy preservation
Replace the single note field in the edit modal with two tabbed timelines: Tech Outreach (owned by Tech Liaison) and Shop Contact Log (owned by Shop Liaison). Both append-only, with revision indicators when an entry is edited.
Tech Outreach entry composer: timestamp (auto), author (auto), outcome enum (Reached / Voicemail / No Answer / Hung Up / Refused), free text, optional BYOV status update (writes byov_status + byov_decision_date only on vrm_repair_tracker — no crossover to vrm_techs or vrm_rental_decisions per R2).
Shop Contact Log entry composer: timestamp (auto), author (auto), optional Shop ETA picker (writes shop_eta_on_road), optional cascading Shop Status update main→sub sourced from shared/fleet-scope-schema.ts (writes main_status + sub_status), optional Van Status update (writes tech_status), free text.
Guardrail 1 — Legacy Notes Preservation (required). For the 6 rows with existing free-form notes (DMCLIEC, ASARWAR, RMADERO, WSCHMI0, TDOHERT, JMORGA1) and for any future row with non-empty notes + empty timelines:

Row indicator: a small "Legacy notes present" italic label in the Tech Outreach preview cell, distinct styling (dashed border or italic muted).
Inside the modal: a read-only "Legacy Notes — not yet classified" panel above the two timeline tabs, showing the original notes text verbatim.
Two buttons on that panel: "Copy to Tech Outreach" and "Copy to Shop Contact Log." Each creates a new timeline entry tagged [migrated from legacy notes], using the original updated_at timestamp if available (otherwise today's date with a note).
Once migrated, the panel and the row indicator disappear automatically.
Never overwrite or delete the original notes field — it stays read-only in the DB as an audit trail.

Step 6 — Actions + CSV export
Add three row-level actions: Close Case (shown only when Stage = Complete; sets closed_at, closed_by, moves row to Completed immediately). Reopen (shown on archived or closed rows; clears closed_at/closed_by, moves back to a live section). Bulk Archive button above the Completed section ("Archive all eligible Completed") — operates on rows complete for 24+ hours.
Preserve the existing Sync Now, Export CSV, and Add Entry buttons. Update Export CSV to include every new field (denial_reason, denial_reason_detail, stage, section, tech_contacted_date, tech_contact_outcome, byov_status, byov_decision_date, shop_last_contacted_date, shop_eta_on_road, route_cleared_date, closed_at, closed_by, link_missing, tech_punch_last_synced_at) plus a section column. CSV uses the existing rental_return_date column — do not introduce rental_returned_date.
Step 7 — AMS drawer polish
The drawer already works. Verify these specific items and fix any gaps:

Section A (Snapshot) mirrors the fleet-management panel exactly — same field order, same groupings, same labels, "AMS last updated {ts} by {updater}" footer, "View in Fleet Management" deep link opens in new tab.
Section B (Comments) shows full history (not the public 20-cap), newest first, color-coded author badges, local time with UTC on hover, search-within box, filter chips (System / Humans / Tech changes / Registration & Renewal / Free text), virtualized list.
Section C (Tracker Notes) renders the two-timeline component from Step 5.
Row-cell indicators on the collapsed row: Truck Status dot, AMS Odometer subline, ⚠ icon when non-operational, hover tooltip "AMS last updated {relative time}".
link_missing rows render "No AMS record for truck #XXXXXX" inline notice and skip the drawer load.
Drawer open time for a 79-comment vehicle: <500 ms cached, <1.5 s cold. Measure and report.
Network trace confirms only GET requests reach AMS from this page.

Step 8 — Acceptance + changelog
Walk through every "Done looks like" bullet from Plan #201 against the deployed UI on real data. For each bullet, attach a screenshot to the changelog. No screenshot = not accepted.
Write the §12 changelog including:

Schema changes (all new columns + new timeline tables).
New endpoints (including GET /api/ams/by-truck/:truckNumber).
Modified endpoints.
Tech Punch Status diagnosis + heuristic documentation (already drafted; reference it).
Complete enumeration of every existing code path that writes to byov_enrolled or any vrm_rental_decisions.byov_* column (per R2 — confirm no writer needs to change).
References to the two follow-up tickets: "Add uniqueness constraint on vrm_repair_tracker(tech_ldap, source_decision_id)" and "Source a real time-punch table from data-platform."
Any AMS fields that could not be surfaced, with reasons.
Route Canceled follow-up call-out (3 legacy rows, dropdown no longer offers the value).

Capture screenshots of: the three populated sections with flag tints visible; the edit modal with both timeline tabs and the Legacy Notes panel on one of the 6 affected rows; an open AMS drawer on a vehicle with many comments; the Export CSV output (header row); the Close Case + Reopen flow on a single case.
Step 9 — Optional snapshot caching (only if approved)
Do not start this without explicit user approval. The cheap win the user described as "porting AMS locally" is to cache a subset of AMS snapshot fields on the tracker row itself so the row-cell indicators and drawer Section A render instantly without a live call. Proposed cached fields: ams_truck_status, ams_odometer, ams_road_ready, ams_how_runs, ams_tech_name, ams_location_city, ams_location_state, ams_last_synced_at. Refresh every 15 minutes for rows in Action Needed + In Progress, lazy on drawer-open for Completed/Archived. Comment history continues to fetch live (it changes too often to cache meaningfully). If approved, this becomes a new follow-up ticket — do not inline it into Steps 4–8.
Non-negotiables across all steps

Do not mark a step Done without a screenshot of the live UI showing the specific acceptance bullet. API verification is not acceptance.
Do not widen scope beyond Plan #201 + this prompt. If you find a structural issue that requires a new decision, stop and ask.
Do not modify AMS write paths, do not write to vrm_techs.byov_enrolled or vrm_rental_decisions.byov_*, do not auto-classify the 6 legacy notes rows.
Do not re-introduce rental_returned_date. Keep using the existing rental_return_date.
Keep the punch-status heuristic disclaimer tooltip intact on the column header.
Report the final row counts per section after Step 8 so they can be compared to Checkpoint 1 numbers.

Delivery
When all of the above is complete, reply with a single message titled "Plan #201 Closeout" containing:

Screenshots as specified.
Link to the updated changelog file.
Final section counts.
Status of the two follow-up tickets (filed Y/N, IDs if filed).
A one-line yes/no on whether Step 9 is recommended based on what you observed during Step 7.

Then reply: PLAN #201 CLOSEOUT COMPLETE — AWAITING FINAL REVIEW and stop. Do not begin Step 9 or any other work without explicit approval.