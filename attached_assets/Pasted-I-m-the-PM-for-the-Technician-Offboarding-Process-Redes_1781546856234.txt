I'm the PM for the Technician Offboarding Process Redesign. This is a BUG-FIX task for the
LOA Asset Recovery notifications. Investigate first, confirm root cause in code, then fix.
Report what you changed and how you verified it. Work in the existing Phases/Nexus patterns —
do not create a standalone app or new abstractions.

BACKGROUND (already diagnosed from the live DB — verify, don't re-derive from scratch):
Two stakeholders received broken "LOA Return Notice" emails (template `loa-return-notice`)
for technician LGARVIN. Confirmed facts:
- All 5 LOA templates (loa-return-notice, loa-team-notice, loa-tech-sms-under30,
  loa-tech-sms-30plus-1, loa-tech-sms-30plus-2) are mode='live' in communication_templates.
- `loa_leaves.return_notice_sent_at` is NULL on EVERY row (incl. LGARVIN, ECAIN, EFIGUE1),
  so duplicate notices fire on each daily sweep.
- ECAIN and EFIGUE1 each have TWO loa_leaves rows with different start_dates.
- LGARVIN's row has van_number = '' (blank) → renders "Not on file".
- loa_team_recipients: fleet=[Jennifer.Dyer], assets=[Jennifer.Dyer, claudia.dominguez],
  inventory=[Jennifer.Dyer, claudia.dominguez]. No Leslie Ellis. No district anywhere.
- There are ZERO communication_logs rows for any loa-* template, ever.

FIX 1 — DUPLICATE / REPEATING ALERTS (highest priority)
Files: server/loa-notifications.ts, server/loa-recovery-sync-service.ts
a) Root-cause why return_notice_sent_at (and team_notice_sent_at) are never persisted even
   though emails went out, AND why there are no communication_logs rows for loa-* templates.
   Trace runLoaNotificationSweep → sendReturnNotice → sendCommunication → createCommunicationLog
   end to end. Determine whether send-state is lost because the send resolves to a status
   other than 'sent'/'simulated', because the row is replaced before the UPDATE, or because
   the sweep/log path is not actually executing. Report the true cause with evidence.
b) Fix the idempotency so a notice sends EXACTLY ONCE per leave episode and survives a
   start-date change. Today workflowId = `loa-{enterpriseId}-{startDate}`, so a changed
   start date creates a brand-new row with empty send-state and re-notifies. Either key the
   leave episode stably (not on startDate), or carry send-state (team_notice_sent_at,
   return_notice_sent_at, tech_sms_sent_at + msg ids) forward onto the new/updated row in
   syncLoaLeaves so prior sends are not repeated.
c) Ensure send-state is recorded whenever a send is actually dispatched (including simulated),
   and that sendCommunication always writes a communication_logs row for these templates.
d) Add a guard so a notice never re-sends once recorded, and confirm `closed` leaves are
   suppressed. Do NOT introduce a manual acknowledgement requirement unless the existing
   patterns already support one.

FIX 2 — VAN "NOT ON FILE"
File: server/loa-recovery-sync-service.ts (syncLoaLeaves van enrichment, ~line 194)
van_number is pulled from getTpmsContact(enterpriseId)?.truckLu — the live TPMS mirror,
which is empty once a tech goes on LOA, so the prior van is lost. Enrich the LAST-KNOWN van
from a source that retains the pre-LOA assignment (Holman / AMS / FleetScope assignment, or a
snapshot captured at LOA start). On refresh, never overwrite a known van_number with blank
(the upsert already COALESCEs — confirm it actually preserves the prior value). Report which
source you used and its coverage.

FIX 3 — DISTRICT NUMBER
Files: shared/schema.ts (loa_leaves), server/loa-recovery-sync-service.ts,
       server/communication-service.ts (loa-return-notice + loa-team-notice templates)
District is not stored or shown, though district/cost-center data exists in the app
(runDistrictCostCenterSeed; the tool-audit SMS template uses {{districtNo}}). Add a
district field to loa_leaves, enrich it by enterpriseId (or via truck/cost-center) from the
existing district source, and add it to both the return-notice and team-notice templates
(HTML + text) as a labeled row. If district can't be resolved for a tech, render a clear
"District: Unknown" rather than omitting the line.

FIX 4 — RECIPIENTS + LESLIE
Add Leslie.Ellis@transformco.com to the fleet and assets recipient lists in
loa_team_recipients (the return notice targets fleet+assets). Confirm the distribution-list
page (client/src/pages/loa-distribution-list.tsx) can manage this. Note: email-service forces
From = the verified SendGrid sender (notifications@shs.com) and sets the passed address as
Reply-To — confirm that is the intended sender for these notices and flag if not.

FIX 5 — DURABLE SIMULATED-MODE SAFETY
The PM will set the LOA templates to mode='simulated' manually. Make that safe: confirm
seedTemplatesOnStartup() (server/index.ts → seed in server/communication-service.ts) does NOT
reset an operator-set `mode` or `is_active` back to the embedded 'live' default on restart.
If it can, change the seed to preserve operator-set mode/is_active (only seed mode on first
insert). Optionally expose a mode toggle on the distribution-list page. Do not change the
current mode values yourself.

VERIFICATION (report results for each):
1. With templates in simulated, run the sweep manually: communication_logs now records
   loa-return-notice rows, and loa_leaves.return_notice_sent_at is set.
2. Run the sweep AGAIN: no duplicate send; send-state unchanged.
3. Simulate a start-date change for one tech: send-state carries forward, no re-send.
4. van_number and district populate for LGARVIN (or a current LOA tech).
5. Leslie appears in the rendered recipient set for the return notice.
6. Restart the app: operator-set template mode is preserved (not reset to live).

REPORT BACK WITH: files changed, the true root cause of the missing send-state/logs, schema
changes, and the test results above. Make no further changes until I review.