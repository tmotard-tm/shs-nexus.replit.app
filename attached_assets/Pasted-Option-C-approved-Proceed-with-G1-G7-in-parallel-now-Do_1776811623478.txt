Option C approved. Proceed with G1–G7 in parallel now. Do NOT wait on me 
for the prod host — I'll paste it the moment I have it, and you wire G8 
immediately after. Do NOT use an empty-allowlist fallback for G8 — it 
stays unbuilt until the real host lands.

Accept and document: historic row-count timeline for vrm_repair_tracker is 
UNRECOVERABLE. Past trends do not exist. The first run of G2 after 
installation IS the baseline going forward. Record this in 
.local/tasks/PRE-DEPLOY-DATA-LOSS-DIAGNOSIS.md under the 
"Unrecoverable Evidence" section so future-you doesn't re-ask.

Parallelize-while-building asks that need platform access (prod 
__drizzle_migrations, Replit deploy audit log): deferred. I will fetch 
these and paste them to you. Do NOT block on them.

Hard constraints (unchanged from prior turn):
- Do NOT run any destructive migration
- Do NOT touch Plan #201 closeout code (Steps 1–8 locked)
- Do NOT Publish
- New scripts under scripts/guardrails/, one file per guardrail
- Show diffs for .replit and package.json BEFORE committing
- All seven guardrails must have DRY-RUN PASS before I approve G8 wiring

Modifications to G1–G7 from my prior turn are all still in effect:
- G1 removes --force, adds destructive-op gate with allow-list file
- G2 writes to durable storage outside Repl workspace, retain last 10
- G3 blocks DROP COLUMN / DROP TABLE / ALTER COLUMN TYPE / RENAME
- G4 per-table tolerance: vrm_repair_tracker ±20%, others ±2%, hard-fail at 50%
- G5 rollback artifact as proposed
- G6 protected_from_dedup BOOLEAN column (persistent, not one-time flag); 
  auto-true on any manual edit to notes/van_status/tech_contacted/byov/
  rental_returned/route_cleared/shop_status; dedup DELETE adds 
  AND protected_from_dedup = false
- G7 direction-safety guard with source-contains-prod / dest-does-not 
  assertions

End state for this pass: GUARDRAILS G1–G7 INSTALLED — AWAITING DRY-RUN 
REVIEW AND PROD HOST FOR G8.

Report format when you finish:
- Per-guardrail: file path, lines changed, DRY-RUN PASS/FAIL, sample output
- Diffs for .replit and package.json
- Any surprises or scope questions surfaced during the build