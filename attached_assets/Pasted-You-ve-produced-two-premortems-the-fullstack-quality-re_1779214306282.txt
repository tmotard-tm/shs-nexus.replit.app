You've produced two premortems (the fullstack quality review and the data‑integrity review). I want you to extend that work and answer it as a CTO would expect, not as a code reviewer. Use the existing findings as input — do not re‑list them. Respond in the exact sections below.

Evidence & confidence table. For every Critical and High finding from both premortems, give me a row with: finding, how you verified it (ran code / reproduced in a scratch env / inferred from replit.md / grep‑only), confidence (High/Medium/Low), and the smallest concrete test or query that would prove it true or false in production today. Flag anything you have not actually reproduced.
Business impact re‑ranking. Re‑rank all Critical/High findings by business impact, not by code severity. For each, estimate: worst‑case blast radius (revenue, customer/tech hours lost, PII exposure, regulatory exposure — SOX, SOC 2, state privacy), which user flows are affected (rental approvals, profitability snapshot, BYOV enrollment, SMS dispatch, admin auth, etc.), and whether the impact is one‑time or compounding. Identify the top 3 findings most likely to cause a real production incident in the next 90 days and justify the pick.
Root‑cause collapse. The current list reads like 15+ independent issues, but several are symptoms of the same underlying problems (identifier normalization sprawl, client/server duplication, schema drift, route‑file bloat, 265 TODOs, inconsistent Zod validation). Collapse the findings into the 3–5 underlying root causes and name the structural fix for each (e.g., canonical identity module, single ORM‑of‑record, shared schema package, routes decomposition, validation at the boundary).
Gap coverage. Add findings for these areas, which the original review did not cover. Be specific with file paths and line numbers where applicable:
a. Observability & incident readiness — logging, alerting, scheduler monitoring, distinct error classes (e.g., "no phone" vs "stale phone"), runbooks, MTTR posture.
b. Data lineage & reconciliation — for every mirrored field (tech_phone, byov_intent, profitability snapshot, Samsara/Holman caches), list source of truth, refresh cadence, staleness window, and whether any reconciliation report exists.
c. AuthN/AuthZ end‑to‑end — map SAML → session → role_permissions. Count endpoints in server/routes.ts and server/fleet-scope-routes.ts that have no authorization check. Document every backdoor or bearer‑token path (emergency admin, VRM_REPAIR_TRACKER_API_KEY, etc.).
d. PII & compliance — what PII flows through the app (phones, employee IDs, SMS content), where it's logged, retention policy, who has prod DB access.
e. Performance & cost — top N+1 query risks, missing indexes, largest payloads, cold‑start behavior on Autoscale (4 vCPU / 8 GiB × 3), and any endpoint likely to saturate the deploy under realistic load.
f. Dependencies & supply chain — outdated packages, known CVEs, abandoned deps, lockfile integrity, build reproducibility.
g. Deploy, rollback, environment parity — is there a staging env with prod‑shaped data? How do we roll back a bad publish? Why have 4+ different developers published to prod in the last 17 days with no apparent gating?
h. Frontend quality — bundle size, accessibility, error boundaries, and correctness of the manual invalidateActiveRentalsCache()‑style invalidation pattern.
i. Test strategy (not coverage) — what kind of tests (contract, property, integration) would catch each of the top 5 risks, and what's the minimum viable test pyramid for this codebase.
j. AI/LLM surface — every call site for OpenAI and Gemini, what data is sent in prompts, prompt‑injection risk, data‑exfiltration risk, and cost controls.
Ownership & process. Independent of code: who owns this repo, is there a code review process, what's the publish gate, and what process changes would have prevented the top 5 findings from landing in the first place? Cheapest fixes usually live here — name them.
Sequenced remediation roadmap. Convert everything above into a plan, not a list. For each remediation item give: effort estimate (S/M/L/XL), dependencies on other items (e.g., "can't refactor routes.ts safely until item #X lands"), the regression test that proves it done, and the owner role suggested (platform, backend, data, frontend, SRE). Group into Now (≤2 weeks), Next (this quarter), Later. Call out which items are reversible vs. one‑way doors.
What you still don't know. End with an explicit "unknowns" list — questions you couldn't answer from the repo alone and need from the team (prod metrics, incident history, contracts with Snowflake/Holman/Samsara, who has DB access, etc.). Do not guess.

Constraints: cite file paths and line numbers for every claim. Where you previously stated a risk without evidence, say so. Do not re‑summarize the existing premortems — assume I've read them.