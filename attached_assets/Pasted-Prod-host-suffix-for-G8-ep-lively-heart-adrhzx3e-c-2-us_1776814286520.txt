Prod host suffix for G8: ep-lively-heart-adrhzx3e.c-2.us-east-1.aws.neon.tech

Wire G8 as follows:

1. Create scripts/guardrails/g8-env-drift-check.ts with a single exported 
   function assertProdDatabaseHost() that:
   - Reads process.env.DATABASE_URL
   - Reads process.env.NODE_ENV
   - If NODE_ENV === "production":
       a. Parse DATABASE_URL via new URL(...)
       b. Assert the .host exactly matches the expected constant
          EXPECTED_PROD_HOST = "ep-lively-heart-adrhzx3e.c-2.us-east-1.aws.neon.tech"
       c. On mismatch: log "[G8] FATAL — prod DATABASE_URL host mismatch. 
          Expected <expected>, got <actual>. Refusing to start." to stderr 
          and call process.exit(1)
       d. On match: log "[G8] OK — prod DB host verified." to stdout
   - If NODE_ENV !== "production": no-op, log "[G8] skipped (NODE_ENV=<value>)."

2. Call assertProdDatabaseHost() at the very top of server/index.ts, 
   BEFORE any DB imports or connection pool initialization. It must run 
   before anything touches the DB.

3. Add a dry-run harness scripts/guardrails/g8-dry-run.ts that simulates:
   - Match case (NODE_ENV=production, correct host) → expect exit 0
   - Mismatch case (NODE_ENV=production, helium host) → expect exit 1
   - Dev case (NODE_ENV=development, any host) → expect no-op exit 0
   Report all three outcomes.

4. Update scripts/guardrails/README.md with a G8 section.

5. Also update G7's default prod-marker list to include the literal 
   substring "ep-lively-heart-adrhzx3e" so G7 will correctly identify 
   this host as prod when deciding refresh direction.

Strict constraints:
- The EXPECTED_PROD_HOST constant is the ONLY place the host string 
  appears in code. Do NOT write it into logs, comments, or test files 
  beyond what's needed for the assertion itself.
- Do NOT store the full DATABASE_URL, the password, the username, or the 
  database name anywhere. The host suffix is the only credential-adjacent 
  value that should exist in the repo.
- If you have the full DATABASE_URL cached in memory or context from 
  earlier, DO NOT write it to any file or commit. Use host-only.
- Do NOT Publish.

Report when done:
- File path and line count for g8-env-drift-check.ts and g8-dry-run.ts
- Dry-run results for all three cases
- Confirmation that G7's prod-marker list was updated
- The line in server/index.ts where assertProdDatabaseHost() is called 
  (must be before any DB-touching import)

End state: GUARDRAIL G8 INSTALLED — AWAITING FULL-SUITE RE-VERIFICATION.