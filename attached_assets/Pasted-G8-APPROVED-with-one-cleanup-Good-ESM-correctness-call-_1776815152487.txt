G8 APPROVED with one cleanup. Good ESM-correctness call on the 
auto-fire pattern — that was the right deviation from my original spec.

Cleanup required before proceeding:

1. Duplicate-file drift risk. server/guardrails/g8-env-drift-check.ts 
   and scripts/guardrails/g8-env-drift-check.ts are identical copies 
   and will silently diverge the first time someone edits one. Fix:

   a. Keep server/guardrails/g8-env-drift-check.ts as canonical.
   b. Delete scripts/guardrails/g8-env-drift-check.ts.
   c. Update scripts/guardrails/g8-dry-run.ts to import from the 
      canonical path:
        import { assertProdDatabaseHost } from "../../server/guardrails/g8-env-drift-check";
      (or the spawn-subprocess equivalent that loads the server-side 
      module — whichever matches your current harness structure)
   d. Re-run the G8 dry-run harness and confirm all three cases still 
      PASS.
   e. Also confirm README.md path reference points at the canonical 
      server/guardrails/ location only.

Then, sequencing for the next step:

2. Run the FULL suite end-to-end (all eight guardrails) as a single 
   command sequence. Report per-guardrail PASS/FAIL in one consolidated 
   output:
     - G1 g1-merge-schema-gate.sh (dry-run mode)
     - G2 g2-pre-deploy-snapshot.ts (dry-run to guardrails/dryrun/)
     - G3 g3-migration-safety-gate.sh (dry-run mode)
     - G4 g4-post-deploy-integrity.ts (expect no-baseline pass)
     - G5 g5-rollback-artifact.ts (dry-run)
     - G6 SELECT to confirm column + trigger still present after any 
         init-schema re-run; idempotency re-apply check
     - G7 both polarities (prod→dev pass, dev→prod block)
     - G8 three-case harness (match / mismatch / dev)

   If any guardrail FAILs, stop and report. If all eight PASS, proceed 
   to step 3.

3. ONLY after the full-suite PASS, wire option α as previously agreed:
     - Update package.json build script with deploy:snapshot + G3 gate 
       + build + deploy:record chained with `&&` (not `;`)
     - Add 4 new npm scripts: deploy:snapshot, deploy:integrity, 
       deploy:record, rollback
     - Update scripts/post-merge.sh to call g1-merge-schema-gate.sh 
       with G1_DRY_RUN=1
     - Add the non-blocking G4 call inside server/index.ts app.listen() 
       callback with [G4] log prefix
     - Show diffs for package.json and scripts/post-merge.sh before 
       committing. No .replit changes.

Do NOT Publish. Do NOT touch Plan #201 UI/route code. Do NOT commit 
anything without showing diffs first.

End state for this pass: FULL-SUITE RE-VERIFIED AND WIRING OPTION α 
DIFFS READY FOR APPROVAL.