Switching from 7 sends to 3 sends to avoid paste truncation. This is message 1 of 3 (points 1–3). Hold consolidated integration until message 3 of 3 arrives.
POINT 1 — TIERED READ ADAPTER (per vendor that has Snowflake stream). Read order for each request: Tier 1: Snowflake — query the latest row keyed by VIN (or vendor asset ID). If row exists AND its last_updated_ts is within the field's freshness threshold, return it. Tag source=snowflake-stream in field_provenance, with last_synced_at = the Snowflake row's timestamp. Tier 2: integration_events — if a webhook event for the same entity is more recent than the Snowflake row, prefer it. Tag source=samsara-webhook (or appropriate vendor). Tier 3: Direct vendor API — only if Tier 1 is missing/stale AND no fresher webhook event exists. Tag source=samsara-api. This call counts against the per-vendor RPS budget. Tier 3 must respect the same token-bucket limiter; if the limiter rejects, return the stale Snowflake value with a stale=true flag rather than throwing. UI can render a "data is N minutes old" indicator using field_provenance.
POINT 2 — FRESHNESS THRESHOLDS (per field, not per vendor). Defaults to put in vendor-limits.ts: GPS lat/lng = 5 min; Speed/heading = 5 min; Last-known address = 30 min; Odometer = 1 hour; Fuel/battery = 1 hour; Driver assignment / vehicle group = 4 hours; VIN/year/make/model/plate = 24 hours. Make these per-field thresholds configurable (env or DB row) so ops can tune without redeploy.
POINT 3 — WEBHOOK ROLE FOR SAMSARA (revise Phase 3B). Shadow mode still happens (parity validation against Snowflake stream). Post-cutover, the Samsara webhook is treated as a CACHE INVALIDATION + recency hint, not the primary data path: if webhook timestamp > Snowflake row timestamp, store the webhook event in integration_events; subsequent reads pick it up via Tier 2. The webhook does NOT force a Tier-3 API call — we rely on the existing 15-min Snowflake refresh to catch up, preserving rate budget for write-side traffic. Tier-3 direct API calls are reserved for: (a) cache miss (no Snowflake row at all), (b) data older than the field's freshness threshold AND no recent webhook, (c) explicit user-initiated "refresh now" actions in the UI.
Acknowledge with just "got 1 of 3" and wait for message 2.
