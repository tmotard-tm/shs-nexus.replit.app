there are three critical operational "blind spots" that aren't fully covered in the current technical requirements:1. The "Race Condition" on Simultaneous WritesWhile Plan #133 adds synchronous verification, it doesn't explicitly define a locking mechanism for a vehicle record.The Risk: If a user clicks "Assign" while the Plan #134 watermark poll is mid-sync, or if two users attempt to assign the same truck at the same second, you could end up with conflicting operation_events.The Fix: Ensure the assignTech function implements a "Processing" state in the holman_vehicles_cache that prevents any other writes (manual or sync-driven) until the current fleet operation is finalized.2. Tech Profile "Ghosting" in TPMSYour plan handles unassigning a tech from a truck, but TPMS often has issues if a tech's Enterprise ID or LDAP changes while they are still assigned to a vehicle in the cache.The Risk: If a tech is termed and then rehired with a new ID, the old record in the tpms_cached_assignments might stay linked to the truck number, causing the Plan #136 dashboard to report a mismatch that isn't actually a "truck" issue, but a "tech data" issue.The Fix: Add a secondary check in Plan #135 to verify that the lastKnownTruckLu tech is still an "Active" status in the TPMS roster before flagging it as a mismatch.3. "Bulk Fix" Rate Limiting & Feedback LoopPlan #136 mentions a "Fix Selected" bulk action.The Risk: AMS and TPMS APIs can be sensitive to rapid-fire POST requests. If a user tries to "Fix All" on 50+ mismatches at once, the downstream APIs might throttle the Nexus service account, leading to a cascade of failed operation events.The Fix: Implement a throttled queue (e.g., 2-second delay between vehicle reconciliations) and ensure the "Progress Sheet" allows the user to cancel the bulk run mid-way if they see a high failure rate.Summary of Missing Logic GatesScenarioCurrent Plan StatusRecommendationUser cancels repairNot in scopeIf a vehicle is "In Repair" but the tech is unassigned, should the AMS status automatically revert to "Spare"?API TimeoutRetry onlyAdd a "Circuit Breaker" to Plan #134 so the watermark poll stops if it hits 3 consecutive timeouts.Historical AuditLogs onlyAdd a "View History" button on the Alignment Dashboard to see how many times a specific truck has "drifted" in the last 30 days.