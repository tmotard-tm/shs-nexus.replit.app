Before implementing Sprint B2, four additions/clarifications to the current plan:

1. Duplicate-send protection
Add idempotency guard to the "Send Outreach" flow. If automationDetail.outreach[] already contains an entry with status 'sent' or 'simulated' for this queue item within the last 4 hours, the frontend should show a confirmation dialog ("Outreach was already sent on [timestamp]. Send again?") before proceeding. This prevents accidental double-sends but does not block legitimate resends.

2. Lane calculation — reuse, don't duplicate
The new /api/assets-queue/:id/send-outreach endpoint MUST import the lane calculation from server/return-token-service.ts (built in Sprint B1). Do not create a third implementation of getDetectionLane. If the function there isn't exported cleanly, refactor to export it — don't copy.

3. Audit trail and variable sourcing
- sentBy on the OutreachEvent must be populated from the authenticated session (req.user.id), not client-supplied
- firstName and technicianName variables must be parsed using parseTechDataFromQueueItem from server/return-token-service.ts — do not write a new tech data parser

4. ADD: Template editor UI
Build a simple template editor so I can update outreach copy without editing seed code or the database directly. Scope:

- New authenticated page at /settings/outreach-templates (or similar — place it wherever fits the existing settings/admin pattern in the app)
- Lists the 4 outreach templates (tool-recovery-outreach-pre/warm/late/cold) in a simple table or card layout
- For each template, allow editing: subject, body (plain text or markdown — whichever matches existing template storage), and mode (simulated / whitelisted / live — dropdown)
- Save button per template writes back to communication_templates table via an authenticated PATCH endpoint
- Show the available variables ({{firstName}}, {{technicianName}}, {{lastDay}}, {{returnLink}}) as a reference so I know what I can use in the copy
- Do NOT build a full CMS — no versioning, no preview, no multi-user editing, no role permissions beyond requireAuth
- Do NOT add template creation or deletion — only the 4 seeded templates are editable
- Link to this page from the assets queue header or a settings menu, whichever exists

Keep the UI minimal — a form per template, save button, done. The goal is letting me update copy and toggle mode between simulated/whitelisted/live without touching code or the DB.

Proceed with these additions incorporated.
