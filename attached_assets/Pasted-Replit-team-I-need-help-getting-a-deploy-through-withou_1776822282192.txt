Replit team — I need help getting a deploy through without a destructive schema migration. Repo-side investigation is done; the remaining unknowns are all in your hosted deploy planner.

Repo: sears-home-services/Nexus
Deployment: https://shs-nexus.replit.app
Prod Neon host: ep-lively-heart-adrhzx3e.c-2.us-east-1.aws.neon.tech
Dev Neon host:  ep-blue-frog-adi73edb.c-2.us-east-1.aws.neon.tech

Problem
The Publish dialog's migration preview shows destructive operations that do not match prod's actual state:
- SET DATA TYPE varchar on offboarding_return_tokens.id
- SET DATA TYPE date on last_known_truck_file_date
- DROP of a truck_lu column that still holds ~13K rows in prod

shared/schema.ts on disk is correct for prod. The only migrations/ file that touches these columns is an unjournaled RENAME, so drizzle-kit migrate would not execute it. Nothing on disk can produce the diff shown.

What I have already confirmed from the repo
- The build chain is: deploy:snapshot → g3-migration-safety-gate → vite build → esbuild → deploy:record. There is no drizzle-kit migrate and no drizzle-kit push anywhere in package.json or the build scripts. So the destructive diff is being generated by your deploy infrastructure, not by my code.
- The running app reads process.env.DATABASE_URL only. Confirmed in server/db.ts line 15 and server/fleet-scope-db.ts line 41. NEON_DATABASE_URL has zero references anywhere in .ts/.js/.json/.sh/.toml source.
- drizzle.config.ts reads process.env.DATABASE_URL only (line 12). Same var as runtime.
- server/guardrails/g8-env-drift-check.ts pins EXPECTED_PROD_HOST = "ep-lively-heart-adrhzx3e.c-2.us-east-1.aws.neon.tech" and refuses to start if DATABASE_URL resolves to any other host. So the running app is guaranteed to be pointed at prod, or it won't boot.

Deployments → Production app secrets pane currently contains (host suffixes only)
- DATABASE_URL       → prod host ep-lively-heart-adrhzx3e...
- PROD_DATABASE_URL  → same prod host (duplicate of DATABASE_URL)
- DEV_DATABASE_URL   → dev host ep-blue-frog-adi73edb...
- NEON_DATABASE_URL  → same dev host (duplicate of DEV_DATABASE_URL)
Plus PGDATABASE / PGHOST / PGPORT / PGUSER / PGPASSWORD (values masked).

Hypothesis I need you to confirm or deny
If your deploy planner reads any env var other than DATABASE_URL to compute the schema diff — specifically if it reads NEON_DATABASE_URL — then it is diffing shared/schema.ts against our DEV database, because NEON_DATABASE_URL in the Deployments pane points at the dev host. That would exactly explain the destructive diff: dev still has the old column types (varchar id, date last_known_truck_file_date, present truck_lu column), so diffing schema.ts against dev produces exactly the three destructive operations we see, and those operations would then be applied to prod on Publish. Meanwhile the running app is unaffected because the g8 guardrail forces DATABASE_URL to prod.

Questions

1. Which env var does your deploy planner read when generating the Publish-dialog schema diff — DATABASE_URL, NEON_DATABASE_URL, the PG* set, or something else? If there is a fallback chain, what is the precedence?

2. Which Neon host is shared/schema.ts currently being diffed against in our Publish preview? Please confirm the host (no password).

3. Your deploy infrastructure is generating this diff — our repo's build chain does not run drizzle-kit push or migrate. How do I disable that planner-side push-style sync entirely, so only our journaled migrations/ (when we choose to run them) can alter the schema?

4. Is "Copy your development database to production database" currently OFF for this deployment, and is it OFF by default on republish?

5. Why does the Deployments "Production app secrets" pane contain NEON_DATABASE_URL pointing at our dev host? Was it placed there by your Neon integration, by the 6/17/2025 auto-sync from the workspace Secrets pane, or something else? How do I unlink the workspace pane from the Deployments pane so future workspace edits stop propagating into prod?

6. If I delete DEV_DATABASE_URL and NEON_DATABASE_URL from the Deployments pane, will anything in your deploy pipeline break? Our app code does not read them.

7. After those two dev-pointing keys are removed, will a fresh Publish attempt diff shared/schema.ts against the prod DB and produce a clean migration preview?

8. Critical for diagnosis: if your planner reads any env var OTHER than DATABASE_URL, confirm that. If it shares the same resolution chain our repo uses (process.env.DATABASE_URL only), then the current diff must be cached/stale and a fresh Publish session should be clean — please confirm which of those two is true.

What I need from you before I publish or delete anything
A. A written statement of which env var the planner uses and which Neon host it is currently diffing against.
B. Confirmation that the next deploy will not run any push-style schema sync against prod — only our repo's build chain will execute.