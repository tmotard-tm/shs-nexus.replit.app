Replit team — I need help getting a deploy through without a destructive schema migration. Before anything is changed or deleted, I need answers to the questions below.

Repo: sears-home-services/Nexus
Deployment: https://shs-nexus.replit.app
Prod Neon host: ep-lively-heart-adrhzx3e.c-2.us-east-1.aws.neon.tech
Dev Neon host:  ep-blue-frog-adi73edb.c-2.us-east-1.aws.neon.tech

Problem:
The Publish dialog's migration preview shows destructive operations that do not match prod's actual state:
- SET DATA TYPE varchar on offboarding_return_tokens.id (prod is already uuid-compatible with the current schema)
- SET DATA TYPE date on last_known_truck_file_date (prod is text)
- DROP of a truck_lu column that still holds ~13K rows in prod

shared/schema.ts on disk is correct for prod. The only migrations/ file that touches these columns is an unjournaled RENAME, so drizzle-kit migrate will not execute it. There is no file on disk that could produce the diff shown.

Questions I need answered before any deploy or secret change:

1. Which environment variable does the deploy planner read to generate the Publish-dialog schema diff — DATABASE_URL, NEON_DATABASE_URL, PG*, or something else? If there is a fallback chain, what is the precedence?

2. Which live database is shared/schema.ts being diffed against in the current Publish preview? Confirm the Neon host (no password).

3. Does the deploy pipeline run journaled Drizzle migrations (drizzle-kit migrate using the migrations/ journal) or a push-style sync (drizzle-kit push equivalent) against the target DB? If push-style, how do I disable it so only journaled migrations run?

4. Is "Copy your development database to production database" currently OFF for this deployment, and is it OFF by default on republish?

5. In the Deployments → Production app secrets pane, DATABASE_URL points at the prod Neon host but NEON_DATABASE_URL points at the dev Neon host. Is that a side-effect of the 6/17/2025 auto-sync change noted in the banner? Which of the two does the deploy planner actually use?

6. Confirm whether the running deployed app reads DATABASE_URL, NEON_DATABASE_URL, or the PG* variables for its DB connection at runtime.

7. Confirm which env var drizzle.config.ts in this repo resolves to at deploy time, since that is what the planner inherits.

What I need from you to move forward:

A. A definitive statement of which env var the planner uses and which DB it is currently diffing against.
B. Confirmation that the upcoming deploy will run only journaled migrations from migrations/ and will NOT run any push-style schema sync.