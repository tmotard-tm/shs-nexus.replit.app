I'll dispatch parallel explorers to audit each blocker pattern across these pages.


5 actions
VRM + FleetScope Performance Audit
Static audit only — no code changed. Findings are grouped by the 5 hypotheses you listed, ranked by likelihood of being the cursor-freeze culprit.

Top suspects (most likely to match your symptom)
🔴 P0 — Large unpaginated JSON payloads → JSON.parse blocks during response handling
This matches your symptom most precisely (freeze "during response handling phase"). Two endpoints dump entire tables:

Endpoint	Where	Problem
GET /api/fs/trucks	server/fleet-scope-routes.ts:2284	storage.getAllTrucks() — entire fs_trucks table. Drives ActiveRentalsDashboard.tsx.
GET /api/fs/all-vehicles	server/fleet-scope-routes.ts:8262	Full vehicle list + BYOV/repair/rental enrichment per row. Drives AllVehicles.tsx.
GET /api/vrm/active-rentals	server/vrm/routes.ts:162	All active rentals × ~60 fields, joined against ~13k-row all_techs.
GET /api/vrm/repair-tracker/full	server/vrm/routes.ts:1088	Full table + LEFT JOIN LATERAL for outreach/contact, with unbounded notes / recommendation text.
GET /api/fs/spares, GET /api/fs/pmf	fleet-scope-routes.ts:6828, 9792	Full table dumps.
ActiveRentalsDashboard.tsx then re-fetches /api/fs/trucks every 60 s (line 256-275) — each refetch re-parses the entire payload and triggers a re-render of the whole table. That is almost certainly your "cursor freezes every minute on the rentals page" pattern.

Fix shape: server-side pagination + field projection. Send ?page=&pageSize=&sort=&filter= from the client; have the server return { rows, total }. For Repair Tracker, drop the lateral-joined long-text fields from the list payload and fetch them lazily on row expand.

🔴 P0 — Synchronous localStorage.setItem + JSON.stringify on every keystroke/filter change
client/src/pages/vehicle-rental-management/pages/ActiveRentalsDashboard.tsx:209-228

useEffect(() => {
  localStorage.setItem(SORT_PREFS_KEY, JSON.stringify({ ... }));
}, [dateInRepairSort, regExpirySort, dailyNetSort, adjNetSort]);
useEffect(() => {
  const prefs: Record<string, unknown> = { ...many filters... };
  localStorage.setItem(FILTER_PREFS_KEY, JSON.stringify(prefs));
}, [ ...many filter deps... ]);

Each filter change → synchronous serialize + disk write on the main thread. Combined with the heavy table re-render right after, this is a strong candidate for input-blocking stutter while typing into search/filter fields.

client/src/pages/fleet-scope/TodaysQueue.tsx:121-125 has the same shape (saveDoneSet writes the whole set on every "Done" toggle).

Fix shape: debounce the writer (200-300 ms trailing) and move it into requestIdleCallback where available; cap stored payload size.

🟠 P1 — Heavy synchronous compute on big arrays (filter / sort / canonicalize) in render
client/src/pages/fleet-management.tsx:1302-1460 — giant useMemo doing string canonicalization + multi-system cross-referencing over the full vehicle array. Re-runs on every search keystroke.
client/src/pages/fleet-management.tsx:2233-2415 — then renders 99 complex rows with per-row distance scoring & multi-system status checks (no virtualization).
client/src/pages/fleet-alignment.tsx:476-503 — [...allRecords].sort(...) in render body, not memoized.
client/src/pages/vehicle-rental-management/pages/Escalations.tsx:125, 223-225 — multiple .filter() calls on rows directly in render.
client/src/pages/vehicle-rental-management/pages/DCAReview.tsx:159, 238 — techs.filter() and unpaginated .map() in JSX.
client/src/pages/fleet-scope/PMF.tsx:498 — JSON.parse(row.rawRow) inside a .map() loop over hundreds of records. This compounds with the 500-row render below.
Fix shape: wrap in useMemo with stable deps; for the canonicalization in fleet-management.tsx, push it server-side or memoize per-vehicle with a WeakMap; pre-parse rawRow once in a query select.

🟠 P1 — Large list / table rendering without virtualization
client/src/pages/fleet-scope/PMF.tsx:1230 — filteredAggregatedAssets.slice(0, 500).map(...) directly inside <TableBody>. 500 multi-cell rows = tens of thousands of DOM nodes per render.
client/src/pages/fleet-management.tsx:2233 — 99 complex row components.
Fix shape: wrap the <TableBody> in react-window's FixedSizeList (or @tanstack/react-virtual since TanStack is already in the stack). PMF is the higher-impact win.

🟡 P2 — Cascading effects / unbatched setState
client/src/pages/fleet-management.tsx:449-461 — 4 setState calls in a useEffect driven by nexusData; combined with sibling effects at lines 382/518/546, clicking a vehicle fires several effect chains. React 18 batches the inner ones, but the chain still produces multiple commits.
ActiveRentalsDashboard.tsx:256-275 — three queries on refetchInterval: 60_000 each invalidating siblings on success → cascade re-renders.
Fix shape: consolidate the per-field setters into a single reducer; stagger refetch intervals or use a single combined query.

Recommended fix priority (biggest cursor-freeze reduction first)
Paginate /api/fs/trucks and /api/vrm/active-rentals — server-side limit/offset/sort/filter, return { rows, total }. This single change kills both the JSON.parse spike and the 60 s recurring freeze on Active Rentals.
Debounce localStorage writers in ActiveRentalsDashboard.tsx and TodaysQueue.tsx.
Virtualize PMF table (PMF.tsx:1230) — swap <TableBody> for @tanstack/react-virtual rows.
Trim Repair Tracker payload — drop the lateral-joined text fields from the list endpoint; load them on row expand.
Memoize the fleet-management.tsx canonicalization useMemo with a stable per-vehicle WeakMap cache so search keystrokes don't re-canonicalize the entire fleet.
Want me to implement any of these?
I held off on diffs because you asked for "audit + findings." Each of the top 5 is a 30-200 line change, and #1 (server pagination) requires touching the client query hooks too. Tell me which to do — I'd recommend tackling #1 + #2 together first since they pair the biggest perceived-freeze fix with the easiest one.


Worked for 2 minutes

Scroll to latest

Task updates
Ready for review