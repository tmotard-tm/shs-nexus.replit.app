Please revise the task with these additions:

1. Add a conflict-check step at the top of the task description:

Before implementing anything, check each target file for any pre-existing offboarding queue code from a previous failed merge attempt. Specifically check: server/routes.ts for existing /api/unified-offboarding/* endpoints or classifyOffboardingQueue/hasOffboardingAccess helpers; client/src/App.tsx for an existing /offboarding-queue route or OffboardingQueue import; shared/page-registry.ts for an existing offboardingQueue entry; client/src/lib/role-permissions.ts for existing offboardingQueue references; server/sync-scheduler.ts and server/run-sync.ts for existing createOffboardingQueueTasks imports; and whether client/src/pages/offboarding-queue.tsx or server/create-offboarding-tasks-service.ts already exist. If any pre-existing code is found, replace it cleanly rather than duplicating it. Also confirm these storage methods exist before relying on them: createNTAOQueueItem, createAssetsQueueItem, createFleetQueueItem, createInventoryQueueItem, getQueueItem, updateQueueItem, createSyncLog, updateSyncLog, getRolePermission. And confirm these imports are available: detectByov, getInitialToolsTaskStatus, TOOLS_OWNER from byov-utils.ts; isSnowflakeConfigured from snowflake-service.ts; getSnowflakeSyncService from snowflake-sync-service.ts.

2. Fill in the truncated subtask details:

The "Backend service" subtask should specify: Assets Management tasks need BYOV logic — call getInitialToolsTaskStatus(truckNumber) to set blockedActions, fleetRoutingDecision, and routingReceivedAt. Set assignedTo to TOOLS_OWNER.id. Phone recovery tasks get extra fields: phoneNumber from tech's contactNumber, phoneRecoveryStage: 'initiation', phoneContactHistory: []. Each task's JSON data field must include: { workflowType: 'offboarding_sequence', step: taskType, subtask, phase: 'day0', isDay0Task: true, source: 'create_offboarding_tasks', syncedAt, technician: {techName, techRacfid, enterpriseId, employeeId, lastDayWorked, effectiveDate, district, planningArea, contactNumber, personalEmail, separationCategory, jobTitle}, vehicle: {vehicleNumber, truckNo, fleetPickupAddress}, instructions: [...] }.

The "API routes" subtask should specify: The PATCH endpoint's allowed field whitelist is: status, notes, assignedTo, priority, taskToolsReturn, taskIphoneReturn, taskDisconnectedLine, taskDisconnectedMPayment, taskCloseSegnoOrders, taskCreateShippingLabel, toolAuditNotificationSent, carrier, fleetRoutingDecision, phoneContactMethod, phoneShippingLabelSent, phoneTrackingNumber, phonePhysicalCondition, phoneConditionNotes, phoneDataWipeCompleted, phoneWipeMethod, phoneReprovisionCompleted, phoneRecoveryStage, phoneWrittenOff, phoneServiceReinstated. Auto-set startedAt when status→in_progress, completedAt when status→completed, toolAuditNotificationSentAt when toolAuditNotificationSent is set. Always set updatedAt. The contact-log endpoint appends { method, notes, contactedBy, timestamp } to the phoneContactHistory array and also updates phoneContactMethod.

The "Frontend page" subtask should specify: The Assets/Tools tab has 6 checklist items: Tools Return Asset ("Verify all assigned tools returned"), iPhone Return Asset ("Check condition and unlock status"), Disconnect Phone Line ("Suspend service"), Deactivate mPayment ("Remove access in Temples system"), Close Segno Orders ("Ensure no open work orders remain"), Create UPS Shipping Label ("Generate QR code for tech"). Quick Actions include Pick Up, Assign, View Tool Audit Form, Send Tool Audit Notification (disabled if no personal email), View in Segno (Coming Soon), Generate Return Label (Coming Soon), and Mark Case Complete (disabled unless all checklist items checked, with warning showing unchecked count). A Phone Carrier dropdown (Verizon/T-Mobile/AT&T) appears only after "Disconnect Phone Line" is checked. The Phone Recovery tab has a Contact History timeline with method icons (phone/email/text/in_person) and outcome badges (Reached/Voicemail/No Response/Wrong Number), plus a Log Contact form. The work email in Contact Details is derived from the tech name if no personal email exists (format: firstname.lastname@company.com).

3. Add an explicit note about the 6 API endpoints (not 5):

There are 6 endpoints total: GET techs list, GET single tech detail, PATCH task update, POST contact log, GET admin duplicate-check, and POST admin run-task-creation. Make sure the subtask count reflects this.