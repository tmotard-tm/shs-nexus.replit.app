Prompt for Replit:
The read-only guard on your prod connection is the correct safety posture — don't try to bypass it or escalate privileges on that connection. Instead, obtain a separate, write-enabled prod connection specifically for this one operation, run Gate 2, then drop it. I don't want to run anything manually in the Neon Console.
Do the following:

Get a write-enabled prod connection. Look at how the deployed app itself connects to prod (the runtime DATABASE_URL used by the production deployment must have write access, since the app writes to prod constantly). Use the same secret/connection string the prod deployment uses, pulled from Replit Secrets or the deployment environment — do NOT hardcode it, do NOT paste it into chat, do NOT log it. If that connection string isn't accessible from your current environment, tell me exactly what's missing and stop; don't improvise.
Sanity-check you're on prod before writing anything. Run these first against the write-enabled connection and confirm the output matches prod (non_null=2005, total=13248):

sql   SELECT current_database();
   SELECT count(*) FILTER (WHERE last_known_truck_file_date IS NOT NULL) AS non_null,
          count(*) AS total
   FROM all_techs;
If the numbers don't match prod's Gate 1 baseline, STOP and report — you're on the wrong database.

Run the Gate 2 DO block exactly as previously specified, through the write-enabled connection:

sql   DO $$
   DECLARE
     v_before_non_null bigint;
     v_before_total    bigint;
     v_after_non_null  bigint;
     v_after_total     bigint;
     v_col_type        text;
   BEGIN
     SELECT count(*) FILTER (WHERE last_known_truck_file_date IS NOT NULL),
            count(*)
       INTO v_before_non_null, v_before_total
       FROM all_techs;
     RAISE NOTICE 'BEFORE: non_null=% total=%', v_before_non_null, v_before_total;

     ALTER TABLE all_techs
       ALTER COLUMN last_known_truck_file_date TYPE date
       USING NULLIF(last_known_truck_file_date, '')::date;

     SELECT count(*) FILTER (WHERE last_known_truck_file_date IS NOT NULL),
            count(*)
       INTO v_after_non_null, v_after_total
       FROM all_techs;

     SELECT data_type INTO v_col_type
       FROM information_schema.columns
       WHERE table_name='all_techs' AND column_name='last_known_truck_file_date';

     RAISE NOTICE 'AFTER:  non_null=% total=% type=%', v_after_non_null, v_after_total, v_col_type;

     IF v_after_non_null <> v_before_non_null THEN
       RAISE EXCEPTION 'ABORT: non_null mismatch before=% after=%', v_before_non_null, v_after_non_null;
     END IF;
     IF v_after_total <> v_before_total THEN
       RAISE EXCEPTION 'ABORT: total mismatch before=% after=%', v_before_total, v_after_total;
     END IF;
     IF v_col_type <> 'date' THEN
       RAISE EXCEPTION 'ABORT: column type is %, expected date', v_col_type;
     END IF;

     RAISE NOTICE 'ALL CHECKS PASSED';
   END $$;

Post-verify on the same connection, independent of the DO block:

sql   SELECT column_name, data_type, udt_name, is_nullable
     FROM information_schema.columns
    WHERE table_name = 'all_techs' AND column_name = 'last_known_truck_file_date';

   SELECT count(*) FILTER (WHERE last_known_truck_file_date IS NOT NULL) AS non_null,
          count(*) AS total,
          min(last_known_truck_file_date) AS oldest,
          max(last_known_truck_file_date) AS newest
     FROM all_techs;

Close the write-enabled connection as soon as the operation completes. Do not leave it open, do not reuse it for anything else, do not cache the connection string anywhere.
Report back with the full output of steps 2, 3, and 4. Do NOT proceed to Gate 3 (shared/schema.ts edit) until I explicitly approve based on the output.

Hard rules still in effect:

Do not bypass the read-only guard on your normal dev connection — get a separate write-enabled connection through the proper secrets mechanism.
Do not print, log, or echo the prod connection string anywhere.
Do not use the "copy dev → prod" deploy option.
Do not edit any file in migrations/.
Do not edit shared/schema.ts yet.
Do not Publish.
If you cannot obtain a write-enabled prod connection through a legitimate mechanism, STOP and tell me what's blocking — do not improvise a workaround.