PRE-IMPLEMENTATION AUDIT — Rental Repair Tracker Rebuild + AMS Integration
Mode: Read-only. Do not modify any files, schemas, migrations, routes, environment variables, or data. Do not install, uninstall, or upgrade packages. Do not run migrations, even in dry-run mode, unless they are explicitly flagged as non-destructive and I approve each one.
Trigger for implementation: You will produce the deliverables below and stop. Wait for me to reply with the exact phrase AUDIT APPROVED — PROCEED before executing anything from the main prompt or the AMS addendum. If I reply with anything else (questions, edits, concerns), continue in read-only mode and refine the audit.

1. Scope of audit
Audit every field, endpoint, service, sync job, and UI component that the main prompt and the AMS addendum propose to add, modify, rename, deprecate, or compute. Specifically cover:

The existing Rental Repair Tracker schema and all proposed additions (denial_reason, stage, tech_contacted_date, tech_contact_outcome, tech_outreach_notes, byov_offered, byov_offered_date, byov_status, byov_decision_date, rental_returned_date, route_cleared_date, shop_last_contacted_date, shop_contact_notes, shop_eta_on_road, assigned_tech_liaison, assigned_shop_liaison, ams_link, closed_at, closed_by).
The existing last_action field (planned deprecation — verify safe to stop writing).
The existing tech_punch_status field and its sync job.
The AMS read endpoints used by /fleet-management that the tracker drawer will reuse (vehicle snapshot + comment history).
The match key truck_number and its normalization rules across tables.
Every other app surface: New Rentals, New Rental – Full Log, Active Rentals, Escalations, DCA Review, Exception Cases, Dashboard, and Fleet Management.

2. Deliverables (produce these as a single Markdown report, no code changes)
2.1 Schema dependency map
For each field listed in §1, list:

Which database tables/columns back it.
Every model, service, controller, route, background job, report, export, or scheduled task that reads it.
Every component, page, modal, or drawer that displays it.
Whether it is emitted in any API response consumed by another page, a CSV export, a webhook, or an external integration.

Present as a table: Field | Table.Column | Read-by (code paths) | Displayed-in (UI paths) | External exposure (Y/N + detail).
2.2 "Safe to deprecate" analysis for last_action
Specifically confirm:

Every place last_action is written.
Every place it is read or displayed.
Whether any report, CSV, or downstream consumer depends on its continued presence.
Whether making it read-only breaks any existing UI or job.
Recommended deprecation path: hard stop, dual-write transition, or retain-as-legacy.

2.3 Shared endpoint / service inventory for AMS
List every existing endpoint, controller, and service that currently serves the fleet-management side panel. For each, state:

Method, path, auth requirements, and shape of response.
Whether it is already reusable as-is for the tracker drawer, or whether it needs refactoring.
Any rate-limit, pagination, or caching considerations.
Whether reusing it from the tracker introduces any permission-leak risk (i.e., could a tracker user see AMS data they cannot see on the fleet page).

2.4 Sync job impact
For the 15-minute Tech Punch Status sync and the existing Sync Now / 7 AM / 1 PM ET tracker sync:

Document the current sources, payloads, and write paths.
Identify any field being added in the main prompt that would require a new sync or extend an existing one.
Flag any risk of the new schema causing sync failures (e.g., non-nullable columns on existing rows, enum mismatches).

2.5 Stage-computation collision check
The main prompt introduces a computed stage field derived from shop_status, van_status, tech_contacted_date, rental_returned, route_cleared, and byov_status. Verify:

None of those source fields are themselves computed or derived elsewhere (which would create a chain).
No existing page already filters, groups, or sorts by a different notion of "stage" that would conflict.
No existing enum values conflict with the proposed ones.

2.6 Truck number normalization check
The addendum specifies leading-zero-preserved, case-insensitive matching on truck_number. Audit:

Storage format in every table that has a truck_number column (rental tracker, fleet/AMS, new rentals, full log, active rentals, escalations, DCA, exceptions).
Any place normalization happens (or doesn't) today.
Whether a shared normalization helper exists; if not, recommend introducing one before the AMS join goes live.

2.7 Cross-section regression risk matrix
Produce a matrix: rows = the other app sections (New Rentals, Full Log, Active Rentals, Escalations, DCA Review, Exception Cases, Dashboard, Fleet Management); columns = proposed changes. Each cell:

None (no interaction), Low (reads but tolerant), Medium (reads and depends on shape), or High (writes or tightly coupled).
Brief justification (one sentence) and the specific code path.

2.8 Migration plan preview
For each schema addition, describe (do not run):

Whether the migration is additive-only (safe) or modifies existing columns (risky).
Default values and nullability.
Backfill strategy and its estimated row count impact.
Rollback steps.

2.9 Open questions / ambiguities
List every ambiguity in the main prompt or the AMS addendum that could reasonably be interpreted two different ways, along with your recommended interpretation. Do not resolve them silently.
2.10 Go/No-go summary
A one-page executive summary with:

Green items (safe to implement as specified).
Yellow items (implementable with minor prompt edits — list the edits).
Red items (require decisions from me before proceeding — list the decisions).

3. Verification methods (read-only only)
You may:

Read source files, migrations, schema definitions, and config.
Run SELECT-only queries against the database to measure row counts, null rates, enum distributions, and orphan rows.
Trace route definitions and import graphs.
Run the app's type checker or linter in report mode.

You may not:

Write, update, or delete any row in any table.
Create, modify, or drop any column, index, constraint, or table.
Push code, open PRs, or change branch state.
Touch environment variables or secrets.
Restart or redeploy any service.

4. Output format
Deliver a single Markdown report named AUDIT-rental-repair-tracker-rebuild.md in the repo root (this is the only file write permitted — and only if writing markdown reports is already a permitted pattern in this repo; otherwise paste the contents into the chat instead). Include at the top:

Date of audit.
Commit SHA audited.
A statement confirming that no non-audit writes were performed.

5. Stop condition
After producing the report, reply with exactly: AUDIT COMPLETE — AWAITING APPROVAL. Do nothing else. Do not begin any work from the main prompt or the AMS addendum until you receive AUDIT APPROVED — PROCEED from me. If you believe the audit reveals that the main prompt or AMS addendum itself needs edits, say so explicitly in the Go/No-go summary and wait.
