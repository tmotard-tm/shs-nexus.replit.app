Do NOT drop, alter, or recreate any existing tables except the one ALTER TABLE ADD COLUMN IF NOT EXISTS described below. Do NOT change shared/fleet-scope-schema.ts. Do NOT change any Fleet Scope pages. All changes are limited to RentalRepairTracker.tsx, server/vrm/storage.ts, server/vrm/init-schema.ts, server/vrm/routes.ts, and shared/vrm-schema.ts.
1. Soft delete — prevent deleted entries from being re-imported:
Add a dismissed boolean column (default false) to vrm_repair_tracker:

In shared/vrm-schema.ts, add dismissed as a boolean column with default false to the vrmRepairTracker table definition
In server/vrm/init-schema.ts, add ALTER TABLE vrm_repair_tracker ADD COLUMN IF NOT EXISTS dismissed BOOLEAN DEFAULT FALSE next to the existing ALTER statements for this table. Do NOT touch any other table.

Update DELETE /api/vrm/repair-tracker/:id in the route handler in server/vrm/routes.ts: instead of actually deleting the row, set dismissed = true on that row. Keep the same HTTP method and response shape.
Update listRepairTracker() in server/vrm/storage.ts: add AND rt.dismissed IS NOT TRUE to the WHERE clause so dismissed entries don't appear in the table.
Update the dedup logic in importDeniedToRepairTracker(): when collecting existing source_decision_id values and existing tech_ldap values for dedup, query ALL rows including dismissed ones. A dismissed entry must still block re-import. The duplicate cleanup query should NOT delete dismissed rows.
2. Remove overridden approvals from the repair tracker:
In importDeniedToRepairTracker() in server/vrm/storage.ts, add a new cleanup step near the beginning:
For each tech_ldap that has a row in vrm_repair_tracker (non-dismissed), check vrm_rental_decisions to see if the most recent decision (by created_at) for that tech_ldap is NOT 'denied' (e.g., it's 'approved'). If so, soft-delete (set dismissed = true) the repair tracker row. This handles techs who were denied then later overridden to approved.
3. UI renames in RentalRepairTracker.tsx:

Rename the "Status" column header to "Shop Status"
Rename the "Tech Status" column header to "Van Status"
In the side panel, rename the "Main Status" dropdown label to "Shop Status"
In the side panel, rename the "Tech Status" dropdown label to "Van Status"

4. Sortable columns with default sort:
In RentalRepairTracker.tsx, add click-to-sort functionality:

Add state for sortColumn (default: "deniedAt") and sortDirection (default: "desc")
Make all column headers clickable. Clicking a header sets it as the sort column; clicking again toggles direction. Show a small arrow (▲ for ascending, ▼ for descending) next to the active sort column header.
Sort the filtered array by the selected column before rendering. For date columns sort by date value, for strings sort alphabetically, for booleans sort true-first or false-first.
Default: denied date, newest first (descending)
This is purely frontend sorting — do NOT change the API query.

Show me exactly what you changed for each numbered item