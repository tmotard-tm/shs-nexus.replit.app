Let me investigate all of those angles right now.

The database has 114 Tools items, but the app crashed. Let me restart it and look at the endpoint code — the issue is likely that the endpoint filters through Snowflake HR separation lookups.

I'm in Plan mode so I can't restart the app, but I've found the root cause. Let me read the Tools Queue endpoint code to confirm my theory:

Now I can see exactly what's happening. Let me also check the separation data query:

Now I have the full picture. Here's what's happening:

The database has 114 Tools items, but the endpoint is not returning them directly. Here's the filtering pipeline that runs every time the Tools Queue page loads:

Step 1 — It calls getAllConfirmedSeparations() which queries the Snowflake SEPARATION_FLEET_DETAILS table to get HR separation records. These are then filtered to only those within the daysBack window (default 30 days).

Step 2 — It fetches all 114 Tools queue_items from the local database.

Step 3 — It enriches each queue item by looking up the tech's enterprise ID or employee ID in the all_techs table and in the HR separation map.

Step 4 (the killer) — It applies a second date filter (lines 2031-2043) that filters the enriched items based on their techData.separationDate. The separation date comes from:

First: The HR SEPARATION_FLEET_DETAILS table (lastDay or effectiveSeparationDate)
Fallback: The all_techs table (lastDayWorked or effectiveDate)
If neither has a date: null — and items with null separation dates pass through the filter (line 2040: if (!sepDate) return true)
So the likely scenario is: Most of the 114 techs' separation dates are older than 30 days (before ~January 10, 2026), so they're being filtered out. Only 1 tech has a separation date within the last 30 days, which is why you see exactly 1 case.

The backfill created the queue_items correctly, but the date filter is too aggressive for historical data. The fix would be to either:

Option A: Use the queue item's created_at date for filtering instead of (or in addition to) the separation date — since these are newly backfilled items, they should be visible regardless of how long ago the tech was termed.

Option B: Default to showing all items (no date filter) or use a longer default window like 90 days.

Option C: Let the date filter apply to the separation date as it does now, but make the default "All" instead of "30 days" so users see everything and can filter down.

Which approach would you prefer? I'd recommend Option A — filter by the queue item's creation date rather than the separation date, since the separation date represents when the tech left, not when the task was created. That way all 114 backfilled items would show up.