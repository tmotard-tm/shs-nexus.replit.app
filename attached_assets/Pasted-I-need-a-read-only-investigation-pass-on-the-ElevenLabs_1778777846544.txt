I need a read-only investigation pass on the ElevenLabs integration in this Nexus Repl.
DO NOT change code, secrets, env vars, DB rows, or deployment state. NO commits. NO pushes.
NO writes of any kind. Multiple owners hold this Repl — any change is high-risk until I've
verified what's actually wired up.

## Background

Earlier today (2026-05-14) the `FS_ELEVENLABS_API_KEY` Replit Secret was overwritten with a
new key belonging to a different ElevenLabs workspace ("Tyler's reactivated workspace",
key ends `944d95319729`). Before that swap, Nexus had been making successful outbound
LUCA repair-shop and tech-coordination calls via these hardcoded values in
`server/fleet-scope-routes.ts`:

- `ELEVENLABS_SHOP_AGENT_ID = "agent_5001kqd4w6c8frqv87rmwqxpxwxy"`
- `ELEVENLABS_TECH_AGENT_ID = "agent_9401kk2njc6veajaecs89wtbh840"`
- `agent_phone_number_id   = "phnum_7001kqfdzf48e9avt82k6259egef"`
- Twilio number used:        `+16677715260` (stored in Secret `FS_TWILIO_PHONE_NUMBER`)

Two SSH-applied code changes were made today, still sitting on the workspace filesystem
but not deployed:
- Both agent IDs above were swapped to `agent_7901kgj8m0w8ep6ar78fzthzr9jv` (SHOP) and
  `agent_4901khvk9569fd2tawwcx0v0hxp5` (TECH). Backup at
  `server/fleet-scope-routes.ts.pre_agent_swap_2026-05-14`.
- The phnum was swapped to a different value and then reverted back to the original
  `phnum_7001kqfdzf48e9avt82k6259egef`. Backup at
  `server/fleet-scope-routes.ts.pre_phnum_swap_2026-05-14`.

When the three original IDs (`agent_5001kqd4...`, `agent_9401kk...`, `phnum_7001...`)
are tested against the NEW key (`944d95319729`), the ElevenLabs API returns 404 Not
Found on all three. That means either (a) the previous `FS_ELEVENLABS_API_KEY` belonged
to a different workspace and the original IDs still live there, or (b) those agents
were moved/deleted between sessions.

I need to know which.

## Investigate and report

1. **Recover the previous `FS_ELEVENLABS_API_KEY` value if possible.**
   Check `deploys/`, `dist/`, any rollback artifacts, any `.env` or `.env.*` files in
   the repo, `.replit.previous`, git history (`git log -p` searches), and any
   deploy-snapshot files generated by `scripts/guardrails/g2-pre-deploy-snapshot.ts` or
   `scripts/guardrails/g5-rollback-artifact.ts`. Report: where you found it, length,
   first 4 chars, last 8 chars. DO NOT paste the full key value in your response.

2. **If you found the previous key, test it against the three original IDs.**
   Run three curl calls:
     curl -s -H "xi-api-key: <previous key>" https://api.elevenlabs.io/v1/convai/agents/agent_5001kqd4w6c8frqv87rmwqxpxwxy
     curl -s -H "xi-api-key: <previous key>" https://api.elevenlabs.io/v1/convai/agents/agent_9401kk2njc6veajaecs89wtbh840
     curl -s -H "xi-api-key: <previous key>" https://api.elevenlabs.io/v1/convai/phone-numbers/phnum_7001kqfdzf48e9avt82k6259egef
   Report HTTP status + the `name` field (for agents) and `phone_number` field (for
   phnum) for each.

3. **Identify the workspace behind the previous key.**
   Run:
     curl -s -H "xi-api-key: <previous key>" https://api.elevenlabs.io/v1/user
     curl -s -H "xi-api-key: <previous key>" https://api.elevenlabs.io/v1/user/subscription
   Report: account first_name, tier, character_limit (or character_count). No private
   fields beyond those three.

4. **Confirm Nexus was actually calling successfully before the key swap.**
   Query the `fs_call_logs` table for rows where `createdAt > now() - interval '14 days'`.
   Report: total row count, breakdown of `status` values, breakdown of `outcome` values,
   the most-recent row's `createdAt` + `status` + `outcome` + `callType`. Goal: confirm
   the pre-swap pipeline was healthy.

5. **Recover the pre-swap key value's history.**
   `git log -p -S "ELEVENLABS_API_KEY" -- .env .env.example .replit deploys/ scripts/`
   `git log -p -S "FS_ELEVENLABS_API_KEY" -- .env .env.example .replit deploys/ scripts/`
   Report any commits where a key value (or placeholder reference to one) changed. Just
   the commit hashes, authors, dates, short messages. No raw key values in output.

6. **Confirm the SSH-applied code changes are still on disk.**
   `ls -la server/fleet-scope-routes.ts.pre_agent_swap_2026-05-14 server/fleet-scope-routes.ts.pre_phnum_swap_2026-05-14`
   `grep -c "agent_7901kgj8m0w8ep6ar78fzthzr9jv" server/fleet-scope-routes.ts`
   `grep -c "agent_4901khvk9569fd2tawwcx0v0hxp5" server/fleet-scope-routes.ts`
   `grep -c "agent_5001kqd4w6c8frqv87rmwqxpxwxy" server/fleet-scope-routes.ts`
   `grep -c "agent_9401kk2njc6veajaecs89wtbh840" server/fleet-scope-routes.ts`
   `grep -n "agent_phone_number_id:" server/fleet-scope-routes.ts`
   Report each verbatim.

7. **List other Nexus collaborators / recent commit authors.**
   `git log --since='90 days ago' --pretty='%an <%ae>' | sort -u`
   Report the list.

## Required output format

Use this structure exactly, no narration:

### Previous key
- Found at: <path or "not recoverable">
- Length / first 4 / last 8: <... / ... / ...>

### Previous key vs original IDs
- agent_5001kqd4...: <HTTP status> <agent name or empty>
- agent_9401kk...:   <HTTP status> <agent name or empty>
- phnum_7001kqfdz...: <HTTP status> <phone number or empty>

### Workspace behind previous key
- Account first_name: <...>
- Tier: <...>
- Character limit/count: <...>

### Call activity, last 14 days
- Total rows: <n>
- status counts: <map>
- outcome counts: <map>
- Most recent: <ts | status | outcome | callType>

### Git history of key/agent-ID changes
- <hash> <author> <date> <subject>
- ...

### Code state on disk
- .pre_agent_swap_2026-05-14: <size, mtime>
- .pre_phnum_swap_2026-05-14: <size, mtime>
- agent_7901kgj8 count in active file: <n>
- agent_4901khvk count in active file: <n>
- agent_5001kqd4 count in active file: <n>
- agent_9401kk count in active file: <n>
- phnum lines: <n lines with the line numbers>

### Other contributors (last 90 days)
- <name <email>>
- ...

### One-line verdict
- Either: "previous key authenticates Workspace X (Collin / other), original IDs live
  there; current new key is on a different workspace; cleanest path is <option>" — or
  whatever the data actually says.

Report only. Make no changes.