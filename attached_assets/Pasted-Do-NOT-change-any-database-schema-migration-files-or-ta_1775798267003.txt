Do NOT change any database schema, migration files, or table definitions. Do NOT change any Fleet Scope pages or shared/fleet-scope-schema.ts.
Auto-populate supervisor fields on the Rental Repair Tracker from tpms_tech_profiles.
1. Update listRepairTracker() in server/vrm/storage.ts:
The query already LEFT JOINs vrm_rental_decisions. Add an additional LEFT JOIN on tpms_tech_profiles matching UPPER(rt.tech_ldap) = UPPER(tpms_tech_profiles.enterprise_id). From this join, pull:

tpms_tech_profiles.tech_manager_name — return as tpmsManagerName
tpms_tech_profiles.tech_manager_ldap_id — use this to do a second LEFT JOIN on tpms_tech_profiles again (aliased as mgr) matching UPPER(mgr.enterprise_id) = UPPER(tpms_tech_profiles.tech_manager_ldap_id) to get the manager's phone number mgr.mobile_phone — return as tpmsManagerPhone

2. Update the frontend in RentalRepairTracker.tsx:
Add tpmsManagerName: string | null and tpmsManagerPhone: string | null to the RepairTrackerEntry interface.
In the table, for the Supervisor column: display entry.supervisorName ?? entry.tpmsManagerName ?? "—". This means manually entered supervisor names take priority, but if none is entered, it falls back to the TPMS data automatically.
Same logic for Sup. Phone: display entry.supervisorPhone ?? entry.tpmsManagerPhone ?? "—".
In the side panel, for the Supervisor Name and Supervisor Phone fields: if the fields are empty and TPMS data exists, show the TPMS value as placeholder text in the input fields so the user can see what the system found, but the actual saved value remains whatever they type (or blank to keep using the auto-populated value).
3. Update the import-denied sync in importDeniedToRepairTracker() in server/vrm/storage.ts:
When inserting new repair tracker rows, also look up the tech's manager from tpms_tech_profiles (the query already looks up truck/phone from this table). Set supervisor_name to tech_manager_name and look up the manager's phone via tech_manager_ldap_id → mobile_phone. This way new entries come pre-populated with supervisor data.
No schema changes. No new tables. No new columns. Just query changes and frontend display logic.