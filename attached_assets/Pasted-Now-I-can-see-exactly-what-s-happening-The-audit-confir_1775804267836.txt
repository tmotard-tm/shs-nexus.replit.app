Now I can see exactly what's happening. The audit confirmed JMORGA1's denial IS in vrm_rental_decisions (timestamp 6:53 AM today), but the sync says "no new entries" — which means the dedup is blocking it.
This is exactly the bug I suspected. Earlier today we added the dismissedTechLdaps set to the dedup blockers — that means once you've been dismissed, your LDAP is permanently blocked from re-import even if you get newly denied.
Here's the fix:


Do NOT change any database schema, migration files, or table definitions. Only modify server/vrm/storage.ts.
Bug: The importDeniedToRepairTracker() function over-blocks re-imports. When a tech is dismissed (soft-deleted) from the repair tracker, their LDAP is added to dismissedTechLdaps, which permanently blocks any future denial for that LDAP from being imported — even when the tech gets a brand new denial decision.
The correct behavior: Dismissing a repair tracker entry should only block re-import of THAT specific decision. If the same tech gets a brand new denial later (a new row in vrm_rental_decisions with a different id), that new decision should create a fresh repair tracker entry.
Fix in importDeniedToRepairTracker():
Find the dedup logic that uses dismissedTechLdaps. Currently it blocks by both dismissedDecisionIds AND dismissedTechLdaps. Change it to ONLY block by dismissedDecisionIds — remove the dismissedTechLdaps blocker entirely.
Specifically:

Keep the query that captures dismissed source_decision_id values into dismissedDecisionIds — this is correct, we want to block re-import of the exact same decision
Remove the dismissedTechLdaps set creation and remove it from the merge into existingTechLdaps
Keep dismissedDecisionIds being merged into existingDecisionIds — this is the correct behavior

The active (non-dismissed) existingTechLdaps check should still apply — if a tech currently has an active row in the tracker, don't create a duplicate. But dismissed rows should NOT contribute to the LDAP blocker.
Show me exactly what you changed.