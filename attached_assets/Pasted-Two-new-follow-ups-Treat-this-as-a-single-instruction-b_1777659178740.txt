Two new follow-ups. Treat this as a single instruction; build both. (1) CONFIGURABLE NOTIFICATION TEMPLATES under VRM > Settings. Goal: let an admin edit the deny-notification SMS body and the deny-notification email subject + body without code changes. STORAGE: add a vrm_settings table (key text pk, value jsonb, updated_by text, updated_at timestamptz default now) OR a single-row vrm_notification_templates table — your call, whichever is cleaner. Persist three settings: sms_template_deny, email_subject_template_deny, email_body_template_deny. UI: new card on the existing VRM Settings tab titled "Deny Notification Templates", placed above the supervisor contact overrides card. For each template show a labeled textarea (richer textarea for email body), a horizontal row of clickable variable chips that insert the variable token at cursor position, a "Reset to default" button, and a Save button. Show a live character count and Twilio segment count under the SMS textarea (160 GSM-7/segment), with amber warning above 320 chars and red above 459. VARIABLES: {{supervisor_first_name}}, {{supervisor_full_name}}, {{tech_first_name}}, {{tech_full_name}}, {{tech_ldap}}, {{decision_date}}. Email-only: {{factors_html}}, {{byov_link}}. Block save if unknown {{...}} tokens are present. RENDER: in notification-dispatcher.ts, before sending, look up the active template, do simple string-replace, fall back to today's hard-coded default if null/empty. Keep all existing behavior: no notifications on Approve, idempotency via UNIQUE(decision_id, channel), skipped-row when both phone and email are absent. (2) NEW RENTALS DASHBOARD COLUMN SORT. Every column header should be a quick-sort toggle that cycles asc → desc → unsorted with a caret indicator; one column at a time; client-side over filtered rows; null/empty values to bottom. Persist last sort (column key + direction) in localStorage per user/page so it restores across navigation/refresh, similar to Active Rentals' existing sort persistence. Apply to every column without exception including text, numeric, date, and status/badge columns. Make sure existing filters and CSV export honor the active sort. Out of scope: multi-column sorting, server-side sort, custom collation. Please apply both and report when done.