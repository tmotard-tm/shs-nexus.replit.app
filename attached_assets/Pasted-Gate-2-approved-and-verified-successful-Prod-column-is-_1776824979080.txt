Gate 2 approved and verified successful. Prod column is now date. Proceed to Gate 3.
Do the following:

Verify current state of shared/schema.ts first. Open the file and confirm line 482 still reads:

ts   // Stored as text in prod (mixed/legacy date formats); parse at read time. Do not change to date().
   lastKnownTruckFileDate: text("last_known_truck_file_date"),
If the line number or content has drifted, find the correct line and report what you found before editing.

Check the imports at the top of shared/schema.ts. Confirm whether date is already imported from drizzle-orm/pg-core. If not, add it to the existing import list — do NOT create a new import line if one already exists for drizzle-orm/pg-core; extend the existing one.
Replace the column definition with:

ts   // Converted in prod from text to date on 2026-04-21 via manual ALTER to unblock deploy planner (dev-as-source-of-truth). mode:"string" preserves existing string-based read contract for all existing read sites.
   lastKnownTruckFileDate: date("last_known_truck_file_date", { mode: "string" }),

Do NOT touch any read sites. The mode: "string" option is specifically chosen so the 13 existing read/write sites identified in Gate 1 continue to work unchanged. If you find yourself tempted to edit any .ts/.tsx file other than shared/schema.ts, STOP and report.
Type-check the project (whatever the equivalent of tsc --noEmit or the project's existing typecheck script is) and confirm zero new type errors are introduced by this change. If any new errors appear, report them — do not attempt to fix them by editing other files. Revert the schema edit and stop.
Do NOT run drizzle-kit push, drizzle-kit generate, or any migration command. We are deliberately letting the planner's deploy-time diff see that prod and dev now match (both date) so the failing ALTER disappears from the generated plan. Any manual migration command could re-emit something destructive.
Do NOT Publish. Gate 4 comes next.
Report back with:

The exact diff of shared/schema.ts (before/after for the edited line and any import change).
The line number where the edit landed.
The typecheck result (pass, or full list of errors if any).
Confirmation that no other files were modified.



Hard rules still in effect:

Do not touch any file in migrations/.
Do not modify any read site for lastKnownTruckFileDate or last_known_truck_file_date.
Do not run any migration generation or push command.
Do not Publish.
If anything unexpected happens, stop and report — do not improvise.

