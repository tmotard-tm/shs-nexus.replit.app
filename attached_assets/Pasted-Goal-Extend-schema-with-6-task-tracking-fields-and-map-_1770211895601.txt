Goal: Extend schema with 6 task tracking fields and map contact info from Snowflake

What You're Building
Add task checklist fields to track Claudia's 6-step workflow. These are DIFFERENT from the blocking logic (Sprints 1-5). Blocking controls WHEN Claudia can act; these tasks track WHAT she completed.

Schema Changes
Add 7 columns to queue_items table in shared/schema.ts:

// Task Checklist (6 boolean fields for Claudia's workflow)
taskToolsReturn: boolean('task_tools_return').default(false),
taskIphoneReturn: boolean('task_iphone_return').default(false),
taskDisconnectedLine: boolean('task_disconnected_line').default(false),
taskDisconnectedMPayment: boolean('task_disconnected_mpayment').default(false),
taskCloseSegnoOrders: boolean('task_close_segno_orders').default(false),
taskCreateShippingLabel: boolean('task_create_shipping_label').default(false),
// Carrier selection (Claudia determines this)
carrier: text('carrier'), // 'Verizon' | 'T-Mobile' | null
Contact Info Mapping
Crystal's Snowflake table provides technician contact info. Map these fields when creating/syncing the Tools task:

Snowflake Field	Queue Item Field	Notes
personal_phone	(display only)	From separation intake form
personal_email	(display only)	For QR code delivery
home_address	(display only)	Current address
Note: Contact info is READ from Snowflake, not stored in queue_items. Query it on demand when displaying the task detail.

API Endpoint
Add endpoint to fetch technician contact info:

// GET /api/tools-queue/:offboardingId/contact
// Returns: { personalPhone, personalEmail, homeAddress, workPhone, workEmail }
// Source: Snowflake separation intake data (Crystal's table)
Constraints
ONLY add schema columns and contact info endpoint
Do NOT build the new UI yet
Do NOT modify the existing task card component
Keep existing blocking logic intact
Acceptance Criteria
 7 new columns exist in queueItems schema (6 tasks + carrier)
 Contact info endpoint returns data from Snowflake
 Existing task cards still work (no regressions)
 Database migration runs successfully
When Done
Report: files changed, what works, any issues, questions for PM.